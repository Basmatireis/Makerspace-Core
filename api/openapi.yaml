openapi: 3.0.3
info:
  title: Makerspace Core API
  version: 0.1.0
  description: |
    HTTP contract for one makerspace deployment. OpenAPI is the transport source
    of truth; authorization and domain invariants remain backend responsibilities.

    Optional nullable fields deliberately distinguish three states: an omitted
    property is unavailable or unchanged, `null` clears or represents no value,
    and a non-null value is present. In particular, `matriculationNumber` is
    omitted from responses unless the caller has `people.read.matriculation`.

    Every session-authenticated operation also accepts the optional
    `X-Managed-Device-Token` credential declared below. OpenAPI security
    requirements cannot express an optional second credential without changing
    their boolean meaning, so the server evaluates this header independently of
    the required session and CSRF credentials.
servers:
  - url: /api/v1
    description: Same-origin application API
tags:
  - name: Health
  - name: Authentication
    description: "Every authentication-route response is emitted with `Cache-Control: no-store`, including errors."
  - name: People
  - name: Accounts
  - name: Permissions
  - name: Roles
  - name: Managed Devices
  - name: Audit
  - name: Open Days
  - name: Public Open Days
  - name: Machines
  - name: Organizations
  - name: Pricing
  - name: Machine Jobs
  - name: Inventory
  - name: Machine Logbook

paths:
  /health/live:
    get:
      tags: [Health]
      operationId: getLiveness
      summary: Report that the API process is running
      security: []
      responses:
        "200":
          description: The process is running.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /health/ready:
    get:
      tags: [Health]
      operationId: getReadiness
      summary: Report whether required dependencies are ready
      security: []
      responses:
        "200":
          description: The API can serve requests.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
        "503":
          description: A required dependency is unavailable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /auth/login:
    post:
      tags: [Authentication]
      operationId: login
      summary: Start an email/password session
      description: |
        On success, sets an HttpOnly session cookie and a separate readable CSRF
        cookie. Authentication failures use one generic response regardless of
        whether the email exists.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "204":
          description: Session created. The browser receives session and CSRF cookies.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
            Set-Cookie:
              description: Session and CSRF cookies. Multiple Set-Cookie headers may be sent.
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          $ref: "#/components/responses/TooManyRequests"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /auth/pin/login:
    post:
      tags: [Authentication]
      operationId: loginWithPin
      summary: Start a low-assurance login-name/PIN session
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PinLoginRequest"
      responses:
        "204":
          description: Authenticated; session and CSRF cookies are set.
          headers:
            Cache-Control: {$ref: "#/components/headers/NoStore"}
            Set-Cookie:
              schema: {type: string}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /auth/methods/pin:
    put:
      tags: [Authentication]
      operationId: enrollOwnPin
      summary: Enroll or replace the current account's PIN method
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/EnrollPinRequest"}
      responses:
        "204": {description: PIN method enrolled and other sessions revoked.}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /auth/pin/enrollment/complete:
    post:
      tags: [Authentication]
      operationId: completePinEnrollment
      summary: Complete a one-time PIN enrollment challenge
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/CompletePinEnrollmentRequest"}
      responses:
        "204": {description: PIN login enrolled; existing sessions were revoked.}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    delete:
      tags: [Authentication]
      operationId: removeOwnPin
      summary: Remove the current account's PIN method
      security:
        - sessionCookie: []
          csrfHeader: []
      responses:
        "204": {description: PIN method removed and other sessions revoked.}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /auth/logout:
    post:
      tags: [Authentication]
      operationId: logout
      summary: Revoke the current session
      security:
        - sessionCookie: []
          csrfHeader: []
      responses:
        "204":
          description: Session revoked and authentication cookies cleared.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
            Set-Cookie:
              description: Expired session and CSRF cookies. Multiple Set-Cookie headers may be sent.
              schema:
                type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /auth/me:
    get:
      tags: [Authentication]
      operationId: getCurrentUser
      summary: Return the authenticated principal and current permissions
      description: Permissions and account state are resolved from current server state.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Current authenticated user.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CurrentUser"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /auth/password:
    put:
      tags: [Authentication]
      operationId: changeOwnPassword
      summary: Change the current account password
      description: Verifies the current password, rotates the current session, and revokes other sessions.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChangeOwnPasswordRequest"
      responses:
        "204":
          description: Password changed and session state rotated.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
            Set-Cookie:
              description: Rotated session and CSRF cookies. Multiple Set-Cookie headers may be sent.
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      tags: [Authentication]
      operationId: removeOwnPassword
      summary: Remove the current account password without stranding the account
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/RemoveOwnPasswordRequest"}
      responses:
        "204": {description: Password removed and all sessions revoked., headers: {Set-Cookie: {schema: {type: string}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /auth/password-reset/request:
    post:
      tags: [Authentication]
      operationId: requestPasswordReset
      summary: Request a password-reset code
      description: Returns the same accepted response for known, unknown, ineligible, rate-limited accounts and delivery failures. Synchronous delivery can affect latency; timing equality is not guaranteed. Malformed requests and infrastructure errors use the documented error responses.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RequestPasswordResetRequest"
      responses:
        "202":
          description: Request accepted without disclosing account or delivery state.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
        "400":
          $ref: "#/components/responses/BadRequest"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /auth/password-reset/complete-code:
    post:
      tags: [Authentication]
      operationId: completePasswordResetCode
      summary: Complete password recovery with an emailed code
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompletePasswordResetCodeRequest"
      responses:
        "204":
          description: Password replaced, code consumed, and existing sessions revoked.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /auth/password-reset/complete:
    post:
      tags: [Authentication]
      operationId: completePasswordReset
      summary: Redeem a one-time password-reset token
      description: The token is accepted only in the request body and is never placed in an API URL.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompletePasswordResetRequest"
      responses:
        "204":
          description: Password replaced, token consumed, existing sessions revoked, and stale authentication cookies cleared.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
            Set-Cookie:
              description: Expired session and CSRF cookies when present. Multiple Set-Cookie headers may be sent.
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /auth/invitations/complete:
    post:
      tags: [Authentication]
      operationId: completeInvitation
      summary: Complete an invitation and set the initial password
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CompleteInvitationRequest"
      responses:
        "204":
          description: Invitation consumed, email verified, password set, and eligible account enabled.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
        "400":
          $ref: "#/components/responses/BadRequest"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /auth/email-verification/request:
    post:
      tags: [Authentication]
      operationId: requestOwnEmailVerification
      summary: Send a verification code for the current account's local login email
      description: The challenge is committed before synchronous delivery is attempted.
      security:
        - sessionCookie: []
          csrfHeader: []
      responses:
        "202":
          description: Verification challenge committed and delivered.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "503": {$ref: "#/components/responses/ServiceUnavailable"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /auth/email-verification/complete:
    post:
      tags: [Authentication]
      operationId: completeEmailVerification
      summary: Verify a local login email with a one-time code
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/CompleteEmailVerificationRequest"}
      responses:
        "204":
          description: Email verified and challenge consumed.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
        "400": {$ref: "#/components/responses/BadRequest"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /people:
    get:
      tags: [People]
      operationId: listPeople
      summary: List people for user administration
      description: Requires `people.read.all`; account data is omitted without `accounts.read`.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/Page"
        - $ref: "#/components/parameters/PageSize"
        - name: search
          in: query
          description: Case-insensitive search over permitted name and contact fields.
          schema:
            type: string
            maxLength: 200
        - name: roleIds
          in: query
          description: |
            Return people assigned to at least one of these roles. Supplying this
            filter requires `accounts.read` because role membership is account data.
          style: form
          explode: true
          schema:
            type: array
            uniqueItems: true
            maxItems: 50
            items:
              $ref: "#/components/schemas/UUIDv7"
      responses:
        "200":
          description: A page of people.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
    post:
      tags: [People]
      operationId: createPerson
      summary: Create a person
      description: Requires `people.create`; matriculation input additionally requires `people.update.matriculation`.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePersonRequest"
      responses:
        "201":
          description: Person created.
          headers:
            Location:
              description: Relative API URL of the created person.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Person"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /people/{personId}:
    parameters:
      - $ref: "#/components/parameters/PersonId"
    get:
      tags: [People]
      operationId: getPerson
      summary: Get one person
      description: Requires the applicable `people.read.self` or `people.read.all` permission.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Person details, filtered for field-level permissions.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Person"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    patch:
      tags: [People]
      operationId: updatePerson
      summary: Update one person
      description: |
        Requires the applicable `people.update.self` or `people.update.all`
        permission. Sending `matriculationNumber` additionally requires
        `people.update.matriculation`. Omitted fields are unchanged; nullable
        fields sent as `null` are cleared.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePersonRequest"
      responses:
        "200":
          description: Updated person.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Person"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      tags: [People]
      operationId: deletePerson
      summary: Permanently delete one person
      description: |
        Requires `people.delete`. If the person has an account, also requires
        `accounts.delete`. Audit references remain non-PII identifiers and do not
        prevent hard deletion.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VersionRequest"
      responses:
        "204":
          description: Person and dependent authentication data permanently deleted.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /people/{personId}/account:
    parameters:
      - $ref: "#/components/parameters/PersonId"
    post:
      tags: [Accounts]
      operationId: createPersonAccount
      summary: Create an account and email identity for a person
      description: Requires `accounts.create`. New accounts are disabled and have no password until explicitly configured.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAccountRequest"
      responses:
        "201":
          description: Account created.
          headers:
            Location:
              description: Relative API URL of the created account.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /people/{personId}/profile-image:
    parameters:
      - $ref: "#/components/parameters/PersonId"
    get:
      tags: [People]
      operationId: getPersonProfileImage
      summary: Read a private normalized profile image
      security: [{sessionCookie: []}]
      responses:
        "200":
          description: Normalized JPEG profile image.
          headers:
            Cache-Control: {$ref: "#/components/headers/NoStore"}
          content:
            image/jpeg:
              schema: {type: string, format: binary}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

    put:
      tags: [People]
      operationId: putPersonProfileImage
      summary: Validate, normalize, and replace a profile image
      security: [{sessionCookie: [], csrfHeader: []}]
      parameters:
        - name: expectedVersion
          in: query
          required: true
          schema: {$ref: "#/components/schemas/Version"}
        - name: X-File-Name
          in: header
          required: true
          schema: {type: string, minLength: 1, maxLength: 255}
        - name: X-Profile-Image-Source
          in: header
          required: false
          schema: {type: string, enum: [terminal_capture, self_upload, admin_upload], default: self_upload}
      requestBody:
        required: true
        description: JPEG, PNG, or WebP bytes. The service verifies the actual decoded format.
        content:
          application/octet-stream: {schema: {type: string, format: binary}}
      responses:
        "200": {description: Updated profile image metadata., content: {application/json: {schema: {$ref: "#/components/schemas/ProfileImage"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "413": {description: Image exceeds 8 MiB.}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    delete:
      tags: [People]
      operationId: deletePersonProfileImage
      summary: Remove a profile image
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "204": {description: Profile image removed.}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /people/{personId}/makerspace-status:
    parameters:
      - $ref: "#/components/parameters/PersonId"
    get:
      tags: [People]
      operationId: getPersonMakerspaceStatus
      summary: Get permission-filtered Makerspace status for one person
      description: |
        Requires the applicable `people.read.self` or `people.read.all`
        permission. Upcoming Open Day assignments are omitted without
        `open_days.read.assignments`; Lab Rules status is omitted without
        `laborordnung.requests.read`.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Makerspace-specific status visible to the caller.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonMakerspaceStatus"
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /accounts/{accountId}:
    parameters:
      - $ref: "#/components/parameters/AccountId"
    get:
      tags: [Accounts]
      operationId: getAccount
      summary: Get one account
      description: Requires `accounts.read`.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Account details.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      tags: [Accounts]
      operationId: deleteAccount
      summary: Permanently delete an account while retaining its person
      description: Requires `accounts.delete` and enforces the last-enabled-master invariant.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VersionRequest"
      responses:
        "204":
          description: Account, identities, sessions, reset tokens, and role assignments deleted.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /accounts/{accountId}/enable:
    parameters:
      - $ref: "#/components/parameters/AccountId"
    post:
      tags: [Accounts]
      operationId: enableAccount
      summary: Enable an account
      description: Requires `accounts.enable`; an active password credential is required.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VersionRequest"
      responses:
        "200":
          description: Enabled account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /accounts/{accountId}/disable:
    parameters:
      - $ref: "#/components/parameters/AccountId"
    post:
      tags: [Accounts]
      operationId: disableAccount
      summary: Disable an account and revoke its sessions
      description: Requires `accounts.disable` and enforces the last-enabled-master invariant.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VersionRequest"
      responses:
        "200":
          description: Disabled account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /accounts/{accountId}/login-email:
    parameters:
      - $ref: "#/components/parameters/AccountId"
    put:
      tags: [Accounts]
      operationId: updateAccountLoginEmail
      summary: Replace an account's email login identifier
      description: Requires `accounts.login_email.update`; this does not silently change the person's contact email.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateLoginEmailRequest"
      responses:
        "200":
          description: Updated account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /accounts/{accountId}/password:
    parameters:
      - $ref: "#/components/parameters/AccountId"
    put:
      tags: [Accounts]
      operationId: setAccountPassword
      summary: Administratively set an account password
      description: Requires `accounts.password.set`; revokes existing sessions and reset tokens without logging the secret.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetAccountPasswordRequest"
      responses:
        "200":
          description: Account with active password credential.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /accounts/{accountId}/password-reset:
    parameters:
      - $ref: "#/components/parameters/AccountId"
    post:
      tags: [Accounts]
      operationId: issueAccountPasswordReset
      summary: Send a one-time administrative password-reset code
      description: |
        Requires `accounts.password.reset`. The code expires after the configured
        reset lifetime (30 minutes by default) and is delivered to the password
        identity. When mail is disabled, an authorized administrator receives a
        one-time setup URL for delivery through a trusted channel.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VersionRequest"
      responses:
        "201":
          description: One-time reset code committed and delivered.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordResetIssue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /accounts/{accountId}/invitations:
    parameters:
      - $ref: "#/components/parameters/AccountId"
    post:
      tags: [Accounts]
      operationId: issueAccountInvitation
      summary: Send or safely resend an account invitation
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VersionRequest"
      responses:
        "201":
          description: Invitation committed and either emailed or returned as a one-time administrator-only setup URL.
          headers:
            Cache-Control:
              $ref: "#/components/headers/NoStore"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PasswordResetIssue"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /accounts/{accountId}/pin-enrollment:
    parameters:
      - $ref: "#/components/parameters/AccountId"
    post:
      tags: [Accounts]
      operationId: issueAccountPinEnrollment
      summary: Send or safely resend a one-time PIN setup challenge
      description: |
        Requires `accounts.pin.enroll.all` when adding PIN login and
        `accounts.pin.reset` when replacing an existing PIN method. The
        administrator never receives or chooses the permanent PIN.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema: {$ref: "#/components/schemas/VersionRequest"}
      responses:
        "201":
          description: PIN setup challenge committed and either emailed or returned as a one-time administrator-only setup URL.
          headers:
            Cache-Control: {$ref: "#/components/headers/NoStore"}
          content:
            application/json:
              schema: {$ref: "#/components/schemas/PasswordResetIssue"}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /accounts/{accountId}/roles/{roleId}:
    parameters:
      - $ref: "#/components/parameters/AccountId"
      - $ref: "#/components/parameters/RoleId"
    put:
      tags: [Accounts]
      operationId: assignAccountRole
      summary: Assign a role to an account
      description: |
        Requires `accounts.roles.assign`. Non-master actors may assign only roles
        whose permission set is a subset of their own effective permissions. Only
        a master actor may assign the master role.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VersionRequest"
      responses:
        "200":
          description: Account with updated roles.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      tags: [Accounts]
      operationId: removeAccountRole
      summary: Remove a role from an account
      description: Requires `accounts.roles.assign`; master removal also enforces master-only and last-enabled-master rules.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VersionRequest"
      responses:
        "200":
          description: Account with updated roles.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Account"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /permissions:
    get:
      tags: [Permissions]
      operationId: listPermissions
      summary: List permissions registered by this application version
      description: Requires `roles.read`; unknown permission identifiers are never accepted for role configuration.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Registered permissions in stable identifier order.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/Permission"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /managed-device-types:
    get:
      tags: [Managed Devices]
      operationId: listManagedDeviceTypes
      summary: List managed-device types
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Device-type catalog., content: {application/json: {schema: {$ref: "#/components/schemas/ManagedDeviceTypeList"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [Managed Devices]
      operationId: createManagedDeviceType
      summary: Create a managed-device type
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreateManagedDeviceTypeRequest"}}}}
      responses:
        "201": {description: Device type created., content: {application/json: {schema: {$ref: "#/components/schemas/ManagedDeviceType"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /managed-device-types/{deviceTypeId}:
    parameters: [{$ref: "#/components/parameters/DeviceTypeId"}]
    get:
      tags: [Managed Devices]
      operationId: getManagedDeviceType
      summary: Get a managed-device type
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Device type., content: {application/json: {schema: {$ref: "#/components/schemas/ManagedDeviceType"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    patch:
      tags: [Managed Devices]
      operationId: updateManagedDeviceType
      summary: Update a managed-device type
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateManagedDeviceTypeRequest"}}}}
      responses:
        "200": {description: Device type updated., content: {application/json: {schema: {$ref: "#/components/schemas/ManagedDeviceType"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    delete:
      tags: [Managed Devices]
      operationId: deleteManagedDeviceType
      summary: Delete an unused managed-device type
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "204": {description: Device type deleted.}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /managed-devices:
    get:
      tags: [Managed Devices]
      operationId: listManagedDevices
      summary: List managed devices
      security: [{sessionCookie: []}]
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
      responses:
        "200": {description: Managed devices., content: {application/json: {schema: {$ref: "#/components/schemas/ManagedDeviceList"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [Managed Devices]
      operationId: createManagedDevice
      summary: Create a managed device and issue its token once
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreateManagedDeviceRequest"}}}}
      responses:
        "201": {description: Device and one-time native token or browser binding., headers: {Cache-Control: {$ref: "#/components/headers/NoStore"}, Set-Cookie: {schema: {type: string}}}, content: {application/json: {schema: {$ref: "#/components/schemas/ManagedDeviceProvisioning"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /managed-devices/{managedDeviceId}:
    parameters: [{$ref: "#/components/parameters/ManagedDeviceId"}]
    get:
      tags: [Managed Devices]
      operationId: getManagedDevice
      summary: Get a managed device
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Managed device., content: {application/json: {schema: {$ref: "#/components/schemas/ManagedDevice"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    patch:
      tags: [Managed Devices]
      operationId: updateManagedDevice
      summary: Update managed-device metadata
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateManagedDeviceRequest"}}}}
      responses:
        "200": {description: Device updated., content: {application/json: {schema: {$ref: "#/components/schemas/ManagedDevice"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    delete:
      tags: [Managed Devices]
      operationId: deleteManagedDevice
      summary: Delete a revoked managed device
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "204": {description: Device deleted.}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /managed-devices/{managedDeviceId}/revoke:
    parameters: [{$ref: "#/components/parameters/ManagedDeviceId"}]
    post:
      tags: [Managed Devices]
      operationId: revokeManagedDevice
      summary: Permanently revoke a managed device
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "200": {description: Revoked device., content: {application/json: {schema: {$ref: "#/components/schemas/ManagedDevice"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /managed-devices/{managedDeviceId}/token:
    parameters: [{$ref: "#/components/parameters/ManagedDeviceId"}]
    post:
      tags: [Managed Devices]
      operationId: rotateManagedDeviceToken
      summary: Rotate a managed-device token
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/RotateManagedDeviceTokenRequest"}}}}
      responses:
        "200": {description: Device and replacement native token or browser binding., headers: {Cache-Control: {$ref: "#/components/headers/NoStore"}, Set-Cookie: {schema: {type: string}}}, content: {application/json: {schema: {$ref: "#/components/schemas/ManagedDeviceProvisioning"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /roles:
    get:
      tags: [Roles]
      operationId: listRoles
      summary: List configured roles
      description: Requires `roles.read`.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
      responses:
        "200":
          description: A page of roles.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RolePage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
    post:
      tags: [Roles]
      operationId: createRole
      summary: Create a configurable role
      description: Requires `roles.manage`; a non-master actor may grant only permissions they currently hold.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRoleRequest"
      responses:
        "201":
          description: Role created.
          headers:
            Location:
              description: Relative API URL of the created role.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /roles/effective-permissions:
    get:
      tags: [Roles]
      operationId: evaluateRolePermissions
      summary: Resolve every role's permissions for a hypothetical request context
      description: |
        Requires `roles.read`. Evaluation uses the same authentication-assurance
        and managed-device rules as request authorization. Omitting `deviceTypeId`
        represents an unmanaged device.
      security:
        - sessionCookie: []
      parameters:
        - name: authenticationAssurance
          in: query
          required: true
          schema:
            $ref: "#/components/schemas/AuthenticationAssurance"
        - name: deviceTypeId
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/UUIDv7"
      responses:
        "200":
          description: Effective permissions for every configured role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoleEffectivePermissionEvaluationList"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /roles/{roleId}:
    parameters:
      - $ref: "#/components/parameters/RoleId"
    get:
      tags: [Roles]
      operationId: getRole
      summary: Get one role
      description: Requires `roles.read`.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Role details.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    patch:
      tags: [Roles]
      operationId: updateRole
      summary: Update role metadata
      description: Requires `roles.manage`. The master role cannot be renamed or edited.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRoleRequest"
      responses:
        "200":
          description: Updated role.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      tags: [Roles]
      operationId: deleteRole
      summary: Delete a configurable role
      description: Requires `roles.manage`. Deletion removes the role's assignments and permission rows; the master role cannot be deleted.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VersionRequest"
      responses:
        "204":
          description: Role deleted.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /roles/{roleId}/permissions:
    parameters:
      - $ref: "#/components/parameters/RoleId"
    put:
      tags: [Roles]
      operationId: replaceRolePermissions
      summary: Replace a configurable role's permissions
      description: |
        Requires `roles.manage`. Unknown permission IDs are rejected. A non-master
        actor may grant only a subset of their effective permissions. The master
        role's effective permissions always come from application code and cannot
        be edited here.
      security:
        - sessionCookie: []
          csrfHeader: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReplaceRolePermissionsRequest"
      responses:
        "200":
          description: Role with replaced permissions.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /audit-events:
    get:
      tags: [Audit]
      operationId: listAuditEvents
      summary: List privacy-minimized audit events
      description: Requires `audit.read`. Audit metadata never contains credentials, tokens, or before/after PII snapshots.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Cursor"
        - name: action
          in: query
          schema:
            type: string
            maxLength: 128
        - name: resourceType
          in: query
          schema:
            type: string
            maxLength: 64
        - name: resourceId
          in: query
          schema:
            $ref: "#/components/schemas/UUIDv7"
        - name: actorAccountId
          in: query
          schema:
            $ref: "#/components/schemas/UUIDv7"
        - name: actorType
          in: query
          schema:
            $ref: "#/components/schemas/AuditActorType"
        - name: actorSearch
          in: query
          description: Case-insensitive search over the current display name of user actors.
          schema:
            type: string
            maxLength: 100
        - name: occurredFrom
          in: query
          schema:
            type: string
            format: date-time
        - name: occurredTo
          in: query
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: A page of audit events ordered newest first.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditEventPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /open-day-periods:
    get:
      tags: [Open Days]
      operationId: listOpenDayPeriods
      summary: List Open Day periods visible to the caller
      security: [{sessionCookie: []}]
      responses:
        "200":
          description: Visible periods with staffing summaries.
          content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayPeriodList"}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [Open Days]
      operationId: createOpenDayPeriod
      summary: Create a draft Open Day period
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: "#/components/schemas/CreateOpenDayPeriodRequest"}}}
      responses:
        "201":
          description: Draft period created.
          content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayPeriod"}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-periods/{periodId}:
    parameters: [{$ref: "#/components/parameters/OpenDayPeriodId"}]
    get:
      tags: [Open Days]
      operationId: getOpenDayPeriod
      summary: Get an Open Day period
      security: [{sessionCookie: []}]
      responses:
        "200":
          description: Period details.
          content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayPeriod"}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    patch:
      tags: [Open Days]
      operationId: updateOpenDayPeriod
      summary: Update draft period metadata
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: "#/components/schemas/UpdateOpenDayPeriodRequest"}}}
      responses:
        "200":
          description: Updated period.
          content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayPeriod"}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-periods/{periodId}/open-for-staffing:
    parameters: [{$ref: "#/components/parameters/OpenDayPeriodId"}]
    post:
      tags: [Open Days]
      operationId: openOpenDayPeriodForStaffing
      summary: Transition a draft period to open for staffing
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody:
        required: true
        content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}
      responses:
        "200": {description: Updated period., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayPeriod"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-periods/{periodId}/return-to-draft:
    parameters: [{$ref: "#/components/parameters/OpenDayPeriodId"}]
    post:
      tags: [Open Days]
      operationId: returnOpenDayPeriodToDraft
      summary: Return a staffing period to draft
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "200": {description: Updated period., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayPeriod"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-periods/{periodId}/publish:
    parameters: [{$ref: "#/components/parameters/OpenDayPeriodId"}]
    post:
      tags: [Open Days]
      operationId: publishOpenDayPeriod
      summary: Publish a staffing period
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "200": {description: Updated period., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayPeriod"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-periods/{periodId}/return-to-staffing:
    parameters: [{$ref: "#/components/parameters/OpenDayPeriodId"}]
    post:
      tags: [Open Days]
      operationId: returnOpenDayPeriodToStaffing
      summary: Unpublish a period and return it to staffing
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "200": {description: Updated period., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayPeriod"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-periods/{periodId}/archive:
    parameters: [{$ref: "#/components/parameters/OpenDayPeriodId"}]
    post:
      tags: [Open Days]
      operationId: archiveOpenDayPeriod
      summary: Archive a published period
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "200": {description: Updated period., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayPeriod"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-periods/{periodId}/open-days:
    parameters: [{$ref: "#/components/parameters/OpenDayPeriodId"}]
    get:
      tags: [Open Days]
      operationId: listOpenDays
      summary: List Open Days in a period
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Period schedule., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDaySchedule"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [Open Days]
      operationId: createOpenDay
      summary: Create one Open Day
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreateOpenDayRequest"}}}}
      responses:
        "201": {description: Open Day created., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDay"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-days/{openDayId}:
    parameters: [{$ref: "#/components/parameters/OpenDayId"}]
    get:
      tags: [Open Days]
      operationId: getOpenDay
      summary: Get one Open Day
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Open Day details., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDay"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    patch:
      tags: [Open Days]
      operationId: updateOpenDay
      summary: Update one scheduled Open Day
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateOpenDayRequest"}}}}
      responses:
        "200": {description: Updated Open Day., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDay"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    delete:
      tags: [Open Days]
      operationId: deleteOpenDay
      summary: Delete one Open Day from a draft period
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "204": {description: Open Day deleted.}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-days/{openDayId}/cancel:
    parameters: [{$ref: "#/components/parameters/OpenDayId"}]
    post:
      tags: [Open Days]
      operationId: cancelOpenDay
      summary: Cancel a scheduled Open Day
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "200": {description: Cancelled Open Day., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDay"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-periods/{periodId}/schedule:
    parameters: [{$ref: "#/components/parameters/OpenDayPeriodId"}]
    put:
      tags: [Open Days]
      operationId: saveOpenDaySchedule
      summary: Atomically save a period schedule delta
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/SaveOpenDayScheduleRequest"}}}}
      responses:
        "200": {description: Saved schedule., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDaySchedule"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-periods/{periodId}/schedule/recurrence-preview:
    parameters: [{$ref: "#/components/parameters/OpenDayPeriodId"}]
    post:
      tags: [Open Days]
      operationId: previewOpenDayRecurrence
      summary: Preview recurring Open Days without persisting them
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/RecurrencePreviewRequest"}}}}
      responses:
        "200": {description: Recurrence preview., content: {application/json: {schema: {$ref: "#/components/schemas/RecurrencePreview"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-days/{openDayId}/assignments/me:
    parameters: [{$ref: "#/components/parameters/OpenDayId"}]
    put:
      tags: [Open Days]
      operationId: joinOpenDay
      summary: Assign the current Person to one staffing requirement
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/JoinOpenDayRequest"}}}}
      responses:
        "201": {description: Assignment created., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayAssignment"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    delete:
      tags: [Open Days]
      operationId: leaveOpenDay
      summary: Remove the current Person's assignment
      security: [{sessionCookie: [], csrfHeader: []}]
      responses:
        "204": {description: Assignment removed.}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-days/{openDayId}/assignments:
    parameters: [{$ref: "#/components/parameters/OpenDayId"}]
    post:
      tags: [Open Days]
      operationId: assignOpenDayPerson
      summary: Administratively assign an eligible Person
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/AssignOpenDayPersonRequest"}}}}
      responses:
        "201": {description: Assignment created., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayAssignment"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-days/{openDayId}/assignments/{assignmentId}:
    parameters:
      - {$ref: "#/components/parameters/OpenDayId"}
      - {$ref: "#/components/parameters/OpenDayAssignmentId"}
    delete:
      tags: [Open Days]
      operationId: removeOpenDayAssignment
      summary: Administratively remove an assignment
      security: [{sessionCookie: [], csrfHeader: []}]
      responses:
        "204": {description: Assignment removed.}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-staff-requirements/{requirementId}/eligible-people:
    parameters: [{$ref: "#/components/parameters/OpenDayRequirementId"}]
    get:
      tags: [Open Days]
      operationId: listOpenDayEligiblePeople
      summary: Search minimal eligible Person identities
      security: [{sessionCookie: []}]
      parameters:
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
      responses:
        "200": {description: Eligible people., content: {application/json: {schema: {$ref: "#/components/schemas/EligiblePersonList"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-eligibility-roles:
    get:
      tags: [Open Days]
      operationId: listOpenDayEligibilityRoles
      summary: List minimal roles usable for Open Day eligibility
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Eligibility roles., content: {application/json: {schema: {$ref: "#/components/schemas/EligibilityRoleList"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-periods/{periodId}/calendar-context:
    parameters: [{$ref: "#/components/parameters/OpenDayPeriodId"}]
    get:
      tags: [Open Days]
      operationId: getOpenDayCalendarContext
      summary: Get holidays and academic breaks for a period
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Calendar context., content: {application/json: {schema: {$ref: "#/components/schemas/OpenDayCalendarContext"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-academic-breaks:
    post:
      tags: [Open Days]
      operationId: createOpenDayAcademicBreak
      summary: Create an academic break
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreateAcademicBreakRequest"}}}}
      responses:
        "201": {description: Academic break created., content: {application/json: {schema: {$ref: "#/components/schemas/AcademicBreak"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /open-day-academic-breaks/{academicBreakId}:
    parameters: [{$ref: "#/components/parameters/AcademicBreakId"}]
    patch:
      tags: [Open Days]
      operationId: updateOpenDayAcademicBreak
      summary: Update an academic break
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateAcademicBreakRequest"}}}}
      responses:
        "200": {description: Academic break updated., content: {application/json: {schema: {$ref: "#/components/schemas/AcademicBreak"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    delete:
      tags: [Open Days]
      operationId: deleteOpenDayAcademicBreak
      summary: Delete an academic break
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "204": {description: Academic break deleted.}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /public/open-days:
    get:
      tags: [Public Open Days]
      operationId: listPublicOpenDays
      summary: List the public Open Day schedule
      security: []
      responses:
        "200": {description: Public schedule only., content: {application/json: {schema: {$ref: "#/components/schemas/PublicOpenDayList"}}}}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /public/open-days/calendar.ics:
    get:
      tags: [Public Open Days]
      operationId: getPublicOpenDaysCalendar
      summary: Subscribe to the public Open Day iCalendar feed
      security: []
      responses:
        "200":
          description: RFC 5545 calendar containing public schedule data only.
          content: {text/calendar: {schema: {type: string}}}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /laborordnung/status/me:
    get:
      tags: [Laborordnung]
      operationId: getOwnLaborordnungStatus
      summary: Read the current Person's Lab Rules status without side effects
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Current compliance status., content: {application/json: {schema: {$ref: "#/components/schemas/LaborordnungStatus"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /laborordnung/requests/me:
    post:
      tags: [Laborordnung]
      operationId: requestOwnLaborordnungConfirmation
      summary: Explicitly create or reuse the current Person's physical-signature request
      security: [{sessionCookie: [], csrfHeader: []}]
      responses:
        "200": {description: Existing pending request reused., content: {application/json: {schema: {$ref: "#/components/schemas/LaborordnungRequest"}}}}
        "201": {description: New pending request created., content: {application/json: {schema: {$ref: "#/components/schemas/LaborordnungRequest"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /laborordnung/versions:
    get:
      tags: [Laborordnung]
      operationId: listLaborordnungVersions
      summary: List Laborordnung history and upcoming publications
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Version history., content: {application/json: {schema: {$ref: "#/components/schemas/LaborordnungVersionList"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [Laborordnung]
      operationId: createLaborordnungVersion
      summary: Validate and store a draft Laborordnung PDF
      security: [{sessionCookie: [], csrfHeader: []}]
      parameters:
        - {name: X-Human-Revision, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 100}}
        - {name: X-File-Name, in: header, required: true, schema: {type: string, minLength: 1, maxLength: 255}}
      requestBody:
        required: true
        content: {application/pdf: {schema: {type: string, format: binary}}}
      responses:
        "201": {description: Draft created., content: {application/json: {schema: {$ref: "#/components/schemas/LaborordnungVersion"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "413": {description: PDF exceeds 25 MiB.}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /laborordnung/versions/{laborordnungVersionId}/publish:
    parameters: [{$ref: "#/components/parameters/LaborordnungVersionId"}]
    post:
      tags: [Laborordnung]
      operationId: publishLaborordnungVersion
      summary: Immutably publish a draft for an effective time
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/PublishLaborordnungRequest"}}}}
      responses:
        "200": {description: Published version., content: {application/json: {schema: {$ref: "#/components/schemas/LaborordnungVersion"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /laborordnung/versions/{laborordnungVersionId}/pdf:
    parameters: [{$ref: "#/components/parameters/LaborordnungVersionId"}]
    get:
      tags: [Laborordnung]
      operationId: getLaborordnungPDF
      summary: View or download the exact private PDF
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Exact published or draft PDF., headers: {Cache-Control: {$ref: "#/components/headers/NoStore"}}, content: {application/pdf: {schema: {type: string, format: binary}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /laborordnung/requests:
    get:
      tags: [Laborordnung]
      operationId: listLaborordnungRequests
      summary: List the supervisor physical-evidence confirmation queue
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Pending and completed requests., content: {application/json: {schema: {$ref: "#/components/schemas/LaborordnungRequestList"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /laborordnung/requests/{laborordnungRequestId}/confirm:
    parameters: [{$ref: "#/components/parameters/LaborordnungRequestId"}]
    post:
      tags: [Laborordnung]
      operationId: confirmLaborordnungRequest
      summary: Confirm verification of a physical signed document
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/ConfirmLaborordnungRequest"}}}}
      responses:
        "200": {description: Completed request., content: {application/json: {schema: {$ref: "#/components/schemas/LaborordnungRequest"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /supervisor-dashboard:
    get:
      tags: [Supervisors]
      operationId: getSupervisorDashboard
      summary: Aggregate designated supervisors, compliance, profile images, and open-period assignment counts
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Privacy-minimized dashboard., content: {application/json: {schema: {$ref: "#/components/schemas/SupervisorDashboard"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /auth/oidc/providers:
    get:
      tags: [OIDC]
      operationId: listOIDCLoginProviders
      summary: List enabled OIDC providers available for login
      security: []
      responses:
        "200": {description: Enabled login providers., content: {application/json: {schema: {$ref: "#/components/schemas/OIDCLoginProviderList"}}}}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /oidc/providers:
    get:
      tags: [OIDC]
      operationId: listOIDCProviders
      summary: List configured OIDC providers without secrets
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Providers., content: {application/json: {schema: {$ref: "#/components/schemas/OIDCProviderList"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [OIDC]
      operationId: createOIDCProvider
      summary: Configure an OIDC provider
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreateOIDCProviderRequest"}}}}
      responses:
        "201": {description: Provider created., content: {application/json: {schema: {$ref: "#/components/schemas/OIDCProvider"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /oidc/providers/{oidcProviderId}:
    parameters: [{$ref: "#/components/parameters/OIDCProviderId"}]
    patch:
      tags: [OIDC]
      operationId: updateOIDCProvider
      summary: Update OIDC provider configuration; omitted client secret is unchanged
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateOIDCProviderRequest"}}}}
      responses:
        "200": {description: Provider updated., content: {application/json: {schema: {$ref: "#/components/schemas/OIDCProvider"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /auth/oidc/{providerSlug}/start:
    get:
      tags: [OIDC]
      operationId: startOIDCLogin
      summary: Start an OIDC Authorization Code flow with PKCE S256
      security: []
      parameters: [{name: providerSlug, in: path, required: true, schema: {type: string, pattern: "^[a-z0-9][a-z0-9-]{1,62}$"}}]
      responses:
        "302": {description: Redirect to the provider., headers: {Location: {schema: {type: string}}, Set-Cookie: {schema: {type: string}}}}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /auth/oidc/callback:
    get:
      tags: [OIDC]
      operationId: completeOIDCCallback
      summary: Validate OIDC callback state, nonce, PKCE, issuer, signature, audience, expiry, and configured ACR
      security: []
      parameters:
        - {name: code, in: query, required: true, schema: {type: string, minLength: 1, maxLength: 4096}}
        - {name: state, in: query, required: true, schema: {type: string, minLength: 20, maxLength: 512}}
      responses:
        "302": {description: Login or linking completed., headers: {Location: {schema: {type: string}}, Set-Cookie: {schema: {type: string}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "409": {$ref: "#/components/responses/Conflict"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /auth/oidc/{providerSlug}/link:
    post:
      tags: [OIDC]
      operationId: startOIDCLink
      summary: Start identity linking with recent normal-or-higher authentication
      description: Requires identities.oidc.link.self and authentication within five minutes. An optional currentPassword refreshes that assurance. Otherwise reauthenticate through an already linked enabled OIDC provider. Low-assurance PIN alone is insufficient. The callback remains bound to the same active session and recent grant.
      security: [{sessionCookie: [], csrfHeader: []}]
      parameters: [{name: providerSlug, in: path, required: true, schema: {type: string, pattern: "^[a-z0-9][a-z0-9-]{1,62}$"}}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/OIDCLinkRequest"}}}}
      responses:
        "200": {description: Authorization URL., content: {application/json: {schema: {$ref: "#/components/schemas/OIDCFlowStart"}}}, headers: {Set-Cookie: {schema: {type: string}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "429": {$ref: "#/components/responses/TooManyRequests"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /auth/oidc/{providerSlug}/reauthenticate:
    post:
      tags: [OIDC]
      operationId: startOIDCReauthentication
      summary: Reauthenticate through an already linked enabled OIDC provider
      description: Uses max_age=0, verified auth_time, and the existing issuer/subject binding. Grants five minutes of recent authentication to the same active session. It cannot provision or link an identity or change the account.
      security: [{sessionCookie: [], csrfHeader: []}]
      parameters: [{name: providerSlug, in: path, required: true, schema: {type: string, pattern: "^[a-z0-9][a-z0-9-]{1,62}$"}}]
      responses:
        "200": {description: Reauthentication authorization URL., content: {application/json: {schema: {$ref: "#/components/schemas/OIDCFlowStart"}}}, headers: {Set-Cookie: {schema: {type: string}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /auth/methods/oidc/{identityId}:
    delete:
      tags: [OIDC]
      operationId: unlinkOwnOIDCIdentity
      summary: Unlink an external identity without stranding the enabled Account
      security: [{sessionCookie: [], csrfHeader: []}]
      parameters: [{name: identityId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}]
      responses:
        "204": {description: Identity unlinked.}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /visitor-enrollment/configuration:
    get:
      tags: [Visitor Enrollment]
      operationId: getVisitorEnrollmentConfiguration
      summary: Read the controlled visitor-terminal configuration
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Configuration., content: {application/json: {schema: {$ref: "#/components/schemas/VisitorEnrollmentConfiguration"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    put:
      tags: [Visitor Enrollment]
      operationId: updateVisitorEnrollmentConfiguration
      summary: Replace the controlled visitor-terminal configuration
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateVisitorEnrollmentConfigurationRequest"}}}}
      responses:
        "200": {description: Updated configuration., content: {application/json: {schema: {$ref: "#/components/schemas/VisitorEnrollmentConfiguration"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /mail/configuration:
    get:
      tags: [Mail]
      operationId: getMailConfiguration
      summary: Read write-only-safe transactional mail configuration
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Mail configuration., content: {application/json: {schema: {$ref: "#/components/schemas/MailConfiguration"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    put:
      tags: [Mail]
      operationId: updateMailConfiguration
      summary: Replace transactional SMTP configuration
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateMailConfigurationRequest"}}}}
      responses:
        "200": {description: Updated mail configuration., content: {application/json: {schema: {$ref: "#/components/schemas/MailConfiguration"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}
  /visitor-enrollment/context:
    post:
      tags: [Visitor Enrollment]
      operationId: beginVisitorEnrollment
      summary: Begin enrollment on an enabled, approved ManagedDevice
      security: []
      responses:
        "204": {description: Enrollment and CSRF cookies created., headers: {Cache-Control: {$ref: "#/components/headers/NoStore"}, Set-Cookie: {schema: {type: string}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /visitor-enrollment/state:
    get:
      tags: [Visitor Enrollment]
      operationId: getVisitorEnrollmentState
      summary: Read enrollment methods and the pinned Lab Rules without creating records
      security: []
      responses:
        "200": {description: Enrollment state., content: {application/json: {schema: {$ref: "#/components/schemas/VisitorEnrollmentState"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /visitor-enrollment/lab-rules.pdf:
    get:
      tags: [Visitor Enrollment]
      operationId: getVisitorEnrollmentLabRulesPDF
      summary: Read the exact Lab Rules PDF pinned when the enrollment context was created
      security: []
      responses:
        "200": {description: Exact pinned PDF; later publications never replace this context's document., content: {application/pdf: {schema: {type: string, format: binary}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /visitor-enrollment/submissions:
    post:
      tags: [Visitor Enrollment]
      operationId: submitVisitorEnrollment
      summary: Atomically create one visitor using the backend-configured Role
      description: Confirmation requests refer to the immutable Lab Rules version pinned by the enrollment context. Current device, method, and initial-role configuration is still enforced.
      security: []
      parameters:
        - {name: X-Enrollment-CSRF-Token, in: header, required: true, schema: {type: string, minLength: 20, maxLength: 512}}
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VisitorEnrollmentSubmission"}}}}
      responses:
        "201": {description: Visitor created; the context is consumed., headers: {Cache-Control: {$ref: "#/components/headers/NoStore"}, Set-Cookie: {schema: {type: string}}}, content: {application/json: {schema: {$ref: "#/components/schemas/VisitorEnrollmentResult"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "413": {$ref: "#/components/responses/PayloadTooLarge"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /visitor-admission:
    post:
      tags: [Visitor Enrollment]
      operationId: evaluateVisitorAdmission
      summary: Explicitly request/reuse required physical confirmation and evaluate admission on an approved ManagedDevice
      security: [{sessionCookie: [], csrfHeader: []}]
      responses:
        "200": {description: Admission decision., content: {application/json: {schema: {$ref: "#/components/schemas/VisitorAdmissionResult"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /scim/connectors:
    get:
      tags: [SCIM Administration]
      operationId: listSCIMConnectors
      summary: List SCIM connectors without bearer-token secrets
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Connectors., content: {application/json: {schema: {$ref: "#/components/schemas/SCIMConnectorList"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [SCIM Administration]
      operationId: createSCIMConnector
      summary: Create a SCIM connector and its first one-time-displayed bearer token
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreateSCIMConnectorRequest"}}}}
      responses:
        "201": {description: Connector and bearer token., headers: {Cache-Control: {$ref: "#/components/headers/NoStore"}}, content: {application/json: {schema: {$ref: "#/components/schemas/SCIMConnectorTokenIssue"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /scim/connectors/{scimConnectorId}:
    parameters: [{name: scimConnectorId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}]
    put:
      tags: [SCIM Administration]
      operationId: updateSCIMConnector
      summary: Update a SCIM connector without changing its token
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateSCIMConnectorRequest"}}}}
      responses:
        "200": {description: Updated connector., content: {application/json: {schema: {$ref: "#/components/schemas/SCIMConnector"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /scim/connectors/{scimConnectorId}/token:
    parameters: [{name: scimConnectorId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}]
    post:
      tags: [SCIM Administration]
      operationId: rotateSCIMConnectorToken
      summary: Revoke the old token and issue a new one-time-displayed bearer token
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/RotateSCIMTokenRequest"}}}}
      responses:
        "201": {description: New token., headers: {Cache-Control: {$ref: "#/components/headers/NoStore"}}, content: {application/json: {schema: {$ref: "#/components/schemas/SCIMConnectorTokenIssue"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    delete:
      tags: [SCIM Administration]
      operationId: revokeSCIMConnectorToken
      summary: Revoke the connector's active bearer token
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "200": {description: Connector with revoked token., content: {application/json: {schema: {$ref: "#/components/schemas/SCIMConnector"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /scim/reconciliation/preflight:
    post:
      tags: [SCIM Administration]
      operationId: preflightSCIMReconciliation
      summary: Analyze a provisional-to-established Account reconciliation without modifying data
      description: The source must be SCIM-provisioned and never authenticated. Every role transfer requires accounts.roles.assign and ordinary privilege-subset delegation. Master roles cannot be transferred. Conflicts are reported without changing either account.
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/SCIMReconciliationRequest"}}}}
      responses:
        "200": {description: Structured conflict analysis., content: {application/json: {schema: {$ref: "#/components/schemas/SCIMReconciliationReport"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /scim/reconciliation:
    post:
      tags: [SCIM Administration]
      operationId: reconcileSCIMAccount
      summary: Atomically reconcile a conflict-free provisional SCIM Account into an established Account
      description: Applies the same lifecycle and role-delegation checks as preflight under transaction locks. scim.manage alone cannot transfer roles, and master roles are never transferred.
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/SCIMReconciliationRequest"}}}}
      responses:
        "200": {description: Completed reconciliation., content: {application/json: {schema: {$ref: "#/components/schemas/SCIMReconciliationReport"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {description: Reconciliation conflicts; no data changed., content: {application/json: {schema: {$ref: "#/components/schemas/SCIMReconciliationReport"}}}}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /scim/v2/ServiceProviderConfig:
    get:
      tags: [SCIM]
      operationId: getSCIMServiceProviderConfig
      security: [{scimBearer: []}]
      responses:
        "200": {description: SCIM service capabilities., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMServiceProviderConfig"}}}}
        "401": {description: Invalid connector token., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}

  /scim/v2/ResourceTypes:
    get:
      tags: [SCIM]
      operationId: listSCIMResourceTypes
      security: [{scimBearer: []}]
      responses:
        "200": {description: Supported resources., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMListResponse"}}}}
        "401": {description: Invalid connector token., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}

  /scim/v2/Schemas:
    get:
      tags: [SCIM]
      operationId: listSCIMSchemas
      security: [{scimBearer: []}]
      responses:
        "200": {description: Supported schemas., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMListResponse"}}}}
        "401": {description: Invalid connector token., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}

  /scim/v2/Users:
    get:
      tags: [SCIM]
      operationId: listSCIMUsers
      security: [{scimBearer: []}]
      parameters:
        - {name: filter, in: query, schema: {type: string, maxLength: 512}}
        - {name: startIndex, in: query, schema: {type: integer, minimum: 1, default: 1}}
        - {name: count, in: query, schema: {type: integer, minimum: 0, maximum: 100, default: 100}}
      responses:
        "200": {description: Connector-scoped Users., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMUserListResponse"}}}}
        "400": {description: Invalid filter., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "401": {description: Invalid connector token., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
    post:
      tags: [SCIM]
      operationId: createSCIMUser
      security: [{scimBearer: []}]
      requestBody: {required: true, content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMUserWrite"}}}}
      responses:
        "201": {description: Provisioned User., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMUser"}}}}
        "400": {description: Invalid User., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "401": {description: Invalid connector token., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "409": {description: Connector-scoped identifier conflict., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}

  /scim/v2/Users/{scimUserId}:
    parameters: [{name: scimUserId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}]
    get:
      tags: [SCIM]
      operationId: getSCIMUser
      security: [{scimBearer: []}]
      responses:
        "200": {description: User., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMUser"}}}}
        "401": {description: Invalid connector token., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "404": {description: User not found., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
    put:
      tags: [SCIM]
      operationId: replaceSCIMUser
      security: [{scimBearer: []}]
      requestBody: {required: true, content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMUserWrite"}}}}
      responses:
        "200": {description: Replaced User., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMUser"}}}}
        "400": {description: Invalid User., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "401": {description: Invalid connector token., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "404": {description: User not found., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "409": {description: Identifier conflict., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
    patch:
      tags: [SCIM]
      operationId: patchSCIMUser
      security: [{scimBearer: []}]
      requestBody: {required: true, content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMPatchRequest"}}}}
      responses:
        "200": {description: Patched User., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMUser"}}}}
        "400": {description: Invalid patch., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "401": {description: Invalid connector token., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "404": {description: User not found., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "409": {description: Identifier conflict., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
    delete:
      tags: [SCIM]
      operationId: deleteSCIMUser
      summary: Deprovision the connector mapping without deleting the Person
      security: [{scimBearer: []}]
      responses:
        "204": {description: Mapping deprovisioned; Person retained.}
        "401": {description: Invalid connector token., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "404": {description: User not found., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}
        "409": {description: Deprovisioning would violate an account security invariant., content: {application/scim+json: {schema: {$ref: "#/components/schemas/SCIMError"}}}}

  /machine-types:
    get:
      tags: [Machines]
      operationId: listMachineTypes
      summary: List machine types
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Machine types., content: {application/json: {schema: {$ref: "#/components/schemas/MachineTypeList"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [Machines]
      operationId: createMachineType
      summary: Create a machine type
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreateMachineTypeRequest"}}}}
      responses:
        "201": {description: Machine type created., content: {application/json: {schema: {$ref: "#/components/schemas/MachineType"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-types/{machineTypeId}:
    parameters:
      - {name: machineTypeId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    patch:
      tags: [Machines]
      operationId: updateMachineType
      summary: Update a machine type
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateMachineTypeRequest"}}}}
      responses:
        "200": {description: Machine type updated., content: {application/json: {schema: {$ref: "#/components/schemas/MachineType"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machines:
    get:
      tags: [Machines]
      operationId: listMachines
      summary: List machines and operational metrics
      security: [{sessionCookie: []}]
      parameters:
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
        - {name: status, in: query, schema: {$ref: "#/components/schemas/MachineStatus"}}
        - {name: page, in: query, schema: {type: integer, minimum: 1, default: 1}}
        - {name: pageSize, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 25}}
      responses:
        "200": {description: Machines., content: {application/json: {schema: {$ref: "#/components/schemas/MachinePage"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [Machines]
      operationId: createMachine
      summary: Create a machine
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreateMachineRequest"}}}}
      responses:
        "201": {description: Machine created., content: {application/json: {schema: {$ref: "#/components/schemas/Machine"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machines/{machineId}:
    parameters:
      - {name: machineId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    get:
      tags: [Machines]
      operationId: getMachine
      summary: Get a machine
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Machine., content: {application/json: {schema: {$ref: "#/components/schemas/Machine"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    patch:
      tags: [Machines]
      operationId: updateMachine
      summary: Update a machine
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateMachineRequest"}}}}
      responses:
        "200": {description: Machine updated., content: {application/json: {schema: {$ref: "#/components/schemas/Machine"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /organizations:
    get:
      tags: [Organizations]
      operationId: listOrganizations
      summary: List customer organizations
      security: [{sessionCookie: []}]
      parameters:
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
        - {name: active, in: query, schema: {type: boolean}}
        - {name: page, in: query, schema: {type: integer, minimum: 1, default: 1}}
        - {name: pageSize, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 25}}
      responses:
        "200": {description: Organizations., content: {application/json: {schema: {$ref: "#/components/schemas/OrganizationPage"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [Organizations]
      operationId: createOrganization
      summary: Create a customer organization
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreateOrganizationRequest"}}}}
      responses:
        "201": {description: Organization created., content: {application/json: {schema: {$ref: "#/components/schemas/Organization"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /organizations/{organizationId}:
    parameters:
      - {name: organizationId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    patch:
      tags: [Organizations]
      operationId: updateOrganization
      summary: Update a customer organization
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateOrganizationRequest"}}}}
      responses:
        "200": {description: Organization updated., content: {application/json: {schema: {$ref: "#/components/schemas/Organization"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /billing-parties:
    get:
      tags: [Organizations]
      operationId: searchBillingParties
      summary: Search minimal people and organizations usable as customers
      security: [{sessionCookie: []}]
      parameters:
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 50, default: 25}}
      responses:
        "200": {description: Matching billing parties., content: {application/json: {schema: {$ref: "#/components/schemas/BillingPartyList"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-job-operators:
    get:
      tags: [Machine Jobs]
      operationId: searchMachineJobOperators
      summary: Search enabled people usable as machine operators
      security: [{sessionCookie: []}]
      parameters:
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
        - {name: limit, in: query, schema: {type: integer, minimum: 1, maximum: 50, default: 25}}
      responses:
        "200": {description: Matching operators., content: {application/json: {schema: {$ref: "#/components/schemas/OperatorList"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /pricing-groups:
    get:
      tags: [Pricing]
      operationId: listPricingGroups
      summary: List pricing groups and rules
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Pricing groups., content: {application/json: {schema: {$ref: "#/components/schemas/PricingGroupList"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [Pricing]
      operationId: createPricingGroup
      summary: Create a pricing group
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreatePricingGroupRequest"}}}}
      responses:
        "201": {description: Pricing group created., content: {application/json: {schema: {$ref: "#/components/schemas/PricingGroup"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /pricing-groups/{pricingGroupId}:
    parameters:
      - {name: pricingGroupId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    patch:
      tags: [Pricing]
      operationId: updatePricingGroup
      summary: Update a pricing group
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdatePricingGroupRequest"}}}}
      responses:
        "200": {description: Pricing group updated., content: {application/json: {schema: {$ref: "#/components/schemas/PricingGroup"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /pricing-groups/{pricingGroupId}/rules:
    parameters:
      - {name: pricingGroupId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    post:
      tags: [Pricing]
      operationId: createPricingRule
      summary: Add a pricing rule
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreatePricingRuleRequest"}}}}
      responses:
        "201": {description: Pricing rule created., content: {application/json: {schema: {$ref: "#/components/schemas/PricingRule"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /pricing-rules/{pricingRuleId}:
    parameters:
      - {name: pricingRuleId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    patch:
      tags: [Pricing]
      operationId: updatePricingRule
      summary: Update a pricing rule
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdatePricingRuleRequest"}}}}
      responses:
        "200": {description: Pricing rule updated., content: {application/json: {schema: {$ref: "#/components/schemas/PricingRule"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /billing-party-pricing-group:
    put:
      tags: [Pricing]
      operationId: setBillingPartyPricingGroup
      summary: Set or clear a billing party default pricing group
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/SetBillingPartyPricingGroupRequest"}}}}
      responses:
        "204": {description: Assignment updated.}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /materials:
    get:
      tags: [Inventory]
      operationId: listMaterials
      summary: List inventory materials and balances
      security: [{sessionCookie: []}]
      parameters:
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
        - {name: category, in: query, schema: {type: string, maxLength: 100}}
        - {name: stockState, in: query, schema: {$ref: "#/components/schemas/StockState"}}
        - {name: page, in: query, schema: {type: integer, minimum: 1, default: 1}}
        - {name: pageSize, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 25}}
      responses:
        "200": {description: Materials., content: {application/json: {schema: {$ref: "#/components/schemas/MaterialPage"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [Inventory]
      operationId: createMaterial
      summary: Create an inventory material
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreateMaterialRequest"}}}}
      responses:
        "201": {description: Material created., content: {application/json: {schema: {$ref: "#/components/schemas/Material"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /materials/{materialId}:
    parameters:
      - {name: materialId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    get:
      tags: [Inventory]
      operationId: getMaterial
      summary: Get material detail
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Material., content: {application/json: {schema: {$ref: "#/components/schemas/Material"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    patch:
      tags: [Inventory]
      operationId: updateMaterial
      summary: Update material metadata
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateMaterialRequest"}}}}
      responses:
        "200": {description: Material updated., content: {application/json: {schema: {$ref: "#/components/schemas/Material"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /materials/{materialId}/transactions:
    parameters:
      - {name: materialId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    get:
      tags: [Inventory]
      operationId: listMaterialTransactions
      summary: List immutable material transactions
      security: [{sessionCookie: []}]
      parameters:
        - {name: page, in: query, schema: {type: integer, minimum: 1, default: 1}}
        - {name: pageSize, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 25}}
      responses:
        "200": {description: Material transactions., content: {application/json: {schema: {$ref: "#/components/schemas/InventoryTransactionPage"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /materials/{materialId}/purchases:
    parameters:
      - {name: materialId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    post:
      tags: [Inventory]
      operationId: addMaterialPurchase
      summary: Add purchased stock and acquisition cost
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/AddMaterialPurchaseRequest"}}}}
      responses:
        "201": {description: Purchase recorded., content: {application/json: {schema: {$ref: "#/components/schemas/InventoryMutationResult"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /materials/{materialId}/consumptions:
    parameters:
      - {name: materialId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    post:
      tags: [Inventory]
      operationId: recordMaterialConsumption
      summary: Record manual consumption or disposal
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/RecordMaterialConsumptionRequest"}}}}
      responses:
        "201": {description: Consumption recorded., content: {application/json: {schema: {$ref: "#/components/schemas/InventoryMutationResult"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /materials/{materialId}/corrections:
    parameters:
      - {name: materialId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    post:
      tags: [Inventory]
      operationId: correctMaterialStock
      summary: Correct stock to a physical count
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CorrectMaterialStockRequest"}}}}
      responses:
        "201": {description: Correction recorded., content: {application/json: {schema: {$ref: "#/components/schemas/InventoryMutationResult"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /materials/{materialId}/mark-empty:
    parameters:
      - {name: materialId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    post:
      tags: [Inventory]
      operationId: markMaterialEmpty
      summary: Mark material empty using an adjustment transaction
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/MarkMaterialEmptyRequest"}}}}
      responses:
        "201": {description: Empty adjustment recorded., content: {application/json: {schema: {$ref: "#/components/schemas/InventoryMutationResult"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-jobs:
    get:
      tags: [Machine Jobs]
      operationId: listMachineJobs
      summary: List and filter machine jobs
      security: [{sessionCookie: []}]
      parameters:
        - {name: search, in: query, schema: {type: string, maxLength: 200}}
        - {name: machineId, in: query, schema: {$ref: "#/components/schemas/UUIDv7"}}
        - {name: customerId, in: query, description: Person or Organization identifier., schema: {$ref: "#/components/schemas/UUIDv7"}}
        - {name: operatorPersonId, in: query, schema: {$ref: "#/components/schemas/UUIDv7"}}
        - {name: materialId, in: query, schema: {$ref: "#/components/schemas/UUIDv7"}}
        - {name: outcome, in: query, schema: {$ref: "#/components/schemas/MachineJobOutcome"}}
        - {name: billingStatus, in: query, schema: {$ref: "#/components/schemas/BillingStatus"}}
        - {name: source, in: query, schema: {$ref: "#/components/schemas/MachineJobSource"}}
        - {name: reviewState, in: query, schema: {$ref: "#/components/schemas/MachineJobReviewState"}}
        - {name: from, in: query, schema: {type: string, format: date-time}}
        - {name: to, in: query, schema: {type: string, format: date-time}}
        - {name: page, in: query, schema: {type: integer, minimum: 1, default: 1}}
        - {name: pageSize, in: query, schema: {type: integer, minimum: 1, maximum: 100, default: 25}}
      responses:
        "200": {description: Machine jobs., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJobPage"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    post:
      tags: [Machine Jobs]
      operationId: createMachineJob
      summary: Create and confirm a manual machine job
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/CreateMachineJobRequest"}}}}
      responses:
        "201": {description: Manual job created., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJob"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-jobs/automatic:
    post:
      tags: [Machine Jobs]
      operationId: ingestAutomaticMachineJob
      summary: Ingest an automatically detected job idempotently
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/IngestAutomaticMachineJobRequest"}}}}
      responses:
        "200": {description: Existing identical job., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJob"}}}}
        "201": {description: Automatic job created., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJob"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-jobs/review-queue:
    get:
      tags: [Machine Jobs]
      operationId: listMachineJobReviewQueue
      summary: List jobs needing review
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Review queue., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJobList"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-jobs/{machineJobId}:
    parameters:
      - {name: machineJobId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    get:
      tags: [Machine Jobs]
      operationId: getMachineJob
      summary: Get machine job detail
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Machine job., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJob"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    patch:
      tags: [Machine Jobs]
      operationId: updateMachineJob
      summary: Correct confirmed machine job facts
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateMachineJobRequest"}}}}
      responses:
        "200": {description: Machine job updated., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJob"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-jobs/{machineJobId}/confirm:
    parameters:
      - {name: machineJobId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    post:
      tags: [Machine Jobs]
      operationId: confirmMachineJob
      summary: Confirm an automatically detected job
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/ConfirmMachineJobRequest"}}}}
      responses:
        "200": {description: Job confirmed., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJob"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-jobs/{machineJobId}/usages:
    parameters:
      - {name: machineJobId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    put:
      tags: [Machine Jobs]
      operationId: replaceMachineJobUsages
      summary: Replace job usages using compensating inventory transactions
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/ReplaceMachineJobUsagesRequest"}}}}
      responses:
        "200": {description: Usages replaced., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJob"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-jobs/{machineJobId}/price-override:
    parameters:
      - {name: machineJobId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    put:
      tags: [Machine Jobs]
      operationId: overrideMachineJobPrice
      summary: Set a final job price without replacing the calculation
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/OverrideMachineJobPriceRequest"}}}}
      responses:
        "200": {description: Price overridden., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJob"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}
    delete:
      tags: [Machine Jobs]
      operationId: clearMachineJobPriceOverride
      summary: Clear the current final-price override
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/VersionRequest"}}}}
      responses:
        "200": {description: Override cleared., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJob"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-jobs/{machineJobId}/billing:
    parameters:
      - {name: machineJobId, in: path, required: true, schema: {$ref: "#/components/schemas/UUIDv7"}}
    put:
      tags: [Machine Jobs]
      operationId: updateMachineJobBilling
      summary: Update the minimal billing state
      security: [{sessionCookie: [], csrfHeader: []}]
      requestBody: {required: true, content: {application/json: {schema: {$ref: "#/components/schemas/UpdateMachineJobBillingRequest"}}}}
      responses:
        "200": {description: Billing state updated., content: {application/json: {schema: {$ref: "#/components/schemas/MachineJob"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "404": {$ref: "#/components/responses/NotFound"}
        "409": {$ref: "#/components/responses/Conflict"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-logbook/overview:
    get:
      tags: [Machine Logbook]
      operationId: getMachineLogbookOverview
      summary: Get operational machine-logbook summary
      security: [{sessionCookie: []}]
      responses:
        "200": {description: Machine-logbook overview., content: {application/json: {schema: {$ref: "#/components/schemas/MachineLogbookOverview"}}}}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "500": {$ref: "#/components/responses/InternalServerError"}

  /machine-logbook/statistics:
    get:
      tags: [Machine Logbook]
      operationId: getMachineLogbookStatistics
      summary: Get date-filtered operational statistics
      security: [{sessionCookie: []}]
      parameters:
        - {name: from, in: query, required: true, schema: {type: string, format: date}}
        - {name: to, in: query, required: true, schema: {type: string, format: date}}
      responses:
        "200": {description: Machine-logbook statistics., content: {application/json: {schema: {$ref: "#/components/schemas/MachineLogbookStatistics"}}}}
        "400": {$ref: "#/components/responses/BadRequest"}
        "401": {$ref: "#/components/responses/Unauthorized"}
        "403": {$ref: "#/components/responses/Forbidden"}
        "422": {$ref: "#/components/responses/UnprocessableEntity"}
        "500": {$ref: "#/components/responses/InternalServerError"}

components:
  headers:
    NoStore:
      description: Prevent browsers and intermediaries from retaining authentication or one-time-secret responses.
      schema:
        type: string
        enum: [no-store]

  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: makerspace_session
      description: Opaque server-side session token. Only a digest is stored; the cookie is HttpOnly. Secure production uses the `__Host-makerspace_session` name.
    csrfHeader:
      type: apiKey
      in: header
      name: X-CSRF-Token
      description: Must exactly mirror `makerspace_csrf` (or secure production `__Host-makerspace_csrf`) on unsafe authenticated requests.
    managedDeviceToken:
      type: apiKey
      in: header
      name: X-Managed-Device-Token
      description: Optional opaque managed-device bearer credential accepted on every session-authenticated operation and injected by a trusted native client. It never replaces the user session.
    scimBearer:
      type: http
      scheme: bearer
      bearerFormat: opaque
      description: One-time-displayed connector token stored only as a digest.

  parameters:
    PersonId:
      name: personId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/UUIDv7"
    AccountId:
      name: accountId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/UUIDv7"
    RoleId:
      name: roleId
      in: path
      required: true
      schema:
        $ref: "#/components/schemas/UUIDv7"
    DeviceTypeId:
      name: deviceTypeId
      in: path
      required: true
      schema: {$ref: "#/components/schemas/UUIDv7"}
    ManagedDeviceId:
      name: managedDeviceId
      in: path
      required: true
      schema: {$ref: "#/components/schemas/UUIDv7"}
    OpenDayPeriodId:
      name: periodId
      in: path
      required: true
      schema: {$ref: "#/components/schemas/UUIDv7"}
    OpenDayId:
      name: openDayId
      in: path
      required: true
      schema: {$ref: "#/components/schemas/UUIDv7"}
    OpenDayRequirementId:
      name: requirementId
      in: path
      required: true
      schema: {$ref: "#/components/schemas/UUIDv7"}
    OpenDayAssignmentId:
      name: assignmentId
      in: path
      required: true
      schema: {$ref: "#/components/schemas/UUIDv7"}
    AcademicBreakId:
      name: academicBreakId
      in: path
      required: true
      schema: {$ref: "#/components/schemas/UUIDv7"}
    LaborordnungVersionId:
      name: laborordnungVersionId
      in: path
      required: true
      schema: {$ref: "#/components/schemas/UUIDv7"}
    LaborordnungRequestId:
      name: laborordnungRequestId
      in: path
      required: true
      schema: {$ref: "#/components/schemas/UUIDv7"}
    OIDCProviderId:
      name: oidcProviderId
      in: path
      required: true
      schema: {$ref: "#/components/schemas/UUIDv7"}
    Limit:
      name: limit
      in: query
      description: Maximum records to return.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 50
    Page:
      name: page
      in: query
      description: One-based result page.
      schema:
        type: integer
        minimum: 1
        default: 1
    PageSize:
      name: pageSize
      in: query
      description: Maximum records to return per page.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 25
    Cursor:
      name: cursor
      in: query
      description: Opaque cursor returned by the previous page.
      schema:
        type: string
        maxLength: 500

  responses:
    BadRequest:
      description: Malformed request, invalid JSON, or unsupported query value.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Unauthorized:
      description: Authentication is missing, expired, revoked, disabled, or invalid.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Forbidden:
      description: The caller lacks permission or the CSRF check failed.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: The requested resource does not exist or is not visible to the caller.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Conflict:
      description: Stale `expectedVersion` (`stale_write`), uniqueness conflict, or protected invariant violation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    UnprocessableEntity:
      description: Structurally valid input violates field or domain validation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    PayloadTooLarge:
      description: The submitted image or document exceeds its route-specific limit.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    TooManyRequests:
      description: Security-sensitive request rate exceeded.
      headers:
        Retry-After:
          description: Seconds until another attempt may be made.
          schema:
            type: integer
            minimum: 1
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    ServiceUnavailable:
      description: A required optional integration is unavailable or delivery failed safely.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    InternalServerError:
      description: Unexpected server error; internal details are not exposed.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

  schemas:
    UUIDv7:
      type: string
      format: uuid
      pattern: "^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$"
      description: Lowercase RFC 9562 UUID version 7 generated by the application.

    Version:
      type: integer
      format: int64
      minimum: 1
      description: Optimistic-concurrency version.

    Email:
      type: string
      format: email
      maxLength: 254

    ExistingPassword:
      type: string
      format: password
      minLength: 1
      maxLength: 128
      writeOnly: true
      description: Opaque existing secret. Transport decoding also enforces a 1024-byte request-field limit.

    NewPassword:
      type: string
      format: password
      minLength: 12
      maxLength: 128
      writeOnly: true

    PermissionId:
      type: string
      enum:
        - people.read.self
        - people.read.all
        - people.create
        - people.update.self
        - people.update.all
        - people.delete
        - people.read.matriculation
        - people.update.matriculation
        - people.profile_image.update.self
        - people.profile_image.update.all
        - people.profile_image.remove.self
        - people.profile_image.remove.all
        - accounts.read
        - accounts.create
        - accounts.delete
        - accounts.enable
        - accounts.disable
        - accounts.login_email.update
        - accounts.password.set
        - accounts.password.reset
        - accounts.password.enroll.self
        - accounts.password.enroll.all
        - accounts.password.remove.self
        - accounts.password.remove.all
        - accounts.pin.enroll.self
        - accounts.pin.enroll.all
        - accounts.pin.remove.self
        - accounts.pin.remove.all
        - accounts.pin.reset
        - accounts.roles.assign
        - roles.read
        - roles.manage
        - audit.read
        - open_days.read
        - open_days.read_assignments
        - open_days.signup
        - open_days.assign
        - open_days.manage
        - managed_devices.read
        - managed_devices.manage
        - laborordnung.read
        - laborordnung.manage
        - laborordnung.requests.read
        - laborordnung.confirm
        - visitor_enrollment.manage
        - supervisor_dashboard.read
        - identities.oidc.link.self
        - identities.oidc.link.all
        - identities.oidc.unlink.self
        - identities.oidc.unlink.all
        - oidc.manage
        - scim.manage
        - mail.manage
        - machines.read
        - machines.manage
        - machine_jobs.read
        - machine_jobs.create
        - machine_jobs.edit
        - machine_jobs.review
        - machine_jobs.override_price
        - inventory.read
        - inventory.manage
        - organizations.read
        - organizations.manage
        - pricing.read
        - pricing.manage
        - statistics.read

    HealthStatus:
      type: object
      additionalProperties: false
      required: [status]
      properties:
        status:
          type: string
          enum: [ok, unavailable]

    Error:
      type: object
      additionalProperties: false
      required: [code, message, requestId]
      properties:
        code:
          type: string
          pattern: "^[a-z][a-z0-9_]*$"
          maxLength: 100
          description: Stable machine-readable error code.
        message:
          type: string
          maxLength: 500
          description: Safe human-readable summary.
        details:
          type: object
          additionalProperties: true
          description: Safe structured context, such as field validation errors.
        requestId:
          type: string
          minLength: 1
          maxLength: 200

    LoginRequest:
      type: object
      additionalProperties: false
      required: [email, password]
      properties:
        email:
          $ref: "#/components/schemas/Email"
        password:
          $ref: "#/components/schemas/ExistingPassword"

    PinLoginRequest:
      type: object
      additionalProperties: false
      required: [loginName, pin]
      properties:
        loginName:
          type: string
          minLength: 3
          maxLength: 64
          pattern: "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        pin:
          type: string
          format: password
          minLength: 6
          maxLength: 12
          pattern: "^[0-9]{6,12}$"
          writeOnly: true

    EnrollPinRequest:
      type: object
      additionalProperties: false
      required: [loginName, pin]
      properties:
        loginName:
          type: string
          minLength: 3
          maxLength: 64
          pattern: "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        pin:
          type: string
          format: password
          minLength: 6
          maxLength: 12
          pattern: "^[0-9]{6,12}$"
          writeOnly: true

    CompletePinEnrollmentRequest:
      type: object
      additionalProperties: false
      required: [accountId, code, loginName, pin]
      properties:
        accountId: {$ref: "#/components/schemas/UUIDv7"}
        code:
          type: string
          minLength: 8
          maxLength: 8
          pattern: "^[23456789ABCDEFGHJKMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz]{8}$"
        loginName:
          type: string
          minLength: 3
          maxLength: 64
          pattern: "^[A-Za-z0-9][A-Za-z0-9._-]*$"
        pin:
          type: string
          format: password
          minLength: 6
          maxLength: 12
          pattern: "^[0-9]{6,12}$"
          writeOnly: true

    ChangeOwnPasswordRequest:
      type: object
      additionalProperties: false
      required: [currentPassword, newPassword]
      properties:
        currentPassword:
          $ref: "#/components/schemas/ExistingPassword"
        newPassword:
          $ref: "#/components/schemas/NewPassword"

    RemoveOwnPasswordRequest:
      type: object
      additionalProperties: false
      required: [currentPassword]
      properties:
        currentPassword: {$ref: "#/components/schemas/ExistingPassword"}

    CompletePasswordResetRequest:
      type: object
      additionalProperties: false
      required: [token, newPassword]
      properties:
        token:
          type: string
          minLength: 32
          maxLength: 512
          writeOnly: true
        newPassword:
          $ref: "#/components/schemas/NewPassword"

    RequestPasswordResetRequest:
      type: object
      additionalProperties: false
      required: [email]
      properties:
        email:
          $ref: "#/components/schemas/Email"

    CompletePasswordResetCodeRequest:
      type: object
      additionalProperties: false
      required: [email, code, newPassword]
      properties:
        email:
          $ref: "#/components/schemas/Email"
        code:
          type: string
          minLength: 8
          maxLength: 8
          pattern: "^[23456789ABCDEFGHJKMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz]{8}$"
        newPassword:
          $ref: "#/components/schemas/NewPassword"

    CompleteInvitationRequest:
      type: object
      additionalProperties: false
      required: [email, code, newPassword]
      properties:
        email:
          $ref: "#/components/schemas/Email"
        code:
          type: string
          minLength: 8
          maxLength: 8
          pattern: "^[23456789ABCDEFGHJKMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz]{8}$"
        newPassword:
          $ref: "#/components/schemas/NewPassword"

    CompleteEmailVerificationRequest:
      type: object
      additionalProperties: false
      required: [email, code]
      properties:
        email:
          $ref: "#/components/schemas/Email"
        code:
          type: string
          minLength: 8
          maxLength: 8
          pattern: "^[23456789ABCDEFGHJKMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz]{8}$"

    CurrentUser:
      type: object
      additionalProperties: false
      required: [account, person, permissions, authenticationAssurance, managedDevice, delegablePermissionGrants, laborordnungStatus]
      properties:
        account:
          $ref: "#/components/schemas/Account"
        person:
          $ref: "#/components/schemas/Person"
        permissions:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/PermissionId"
        authenticationAssurance:
          $ref: "#/components/schemas/AuthenticationAssurance"
        managedDevice:
          allOf: [{$ref: "#/components/schemas/ManagedDeviceContext"}]
          nullable: true
        delegablePermissionGrants:
          type: array
          items: {$ref: "#/components/schemas/PermissionGrant"}
        laborordnungStatus:
          $ref: "#/components/schemas/LaborordnungStatus"

    ManagedDeviceContext:
      type: object
      additionalProperties: false
      required: [id, name, deviceTypeId, deviceTypeName, expiresAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string}
        deviceTypeId: {$ref: "#/components/schemas/UUIDv7"}
        deviceTypeName: {type: string}
        expiresAt: {type: string, format: date-time, nullable: true}

    Person:
      type: object
      additionalProperties: false
      required: [id, firstName, lastName, createdAt, updatedAt, version]
      properties:
        id:
          $ref: "#/components/schemas/UUIDv7"
        firstName:
          type: string
          minLength: 1
          maxLength: 100
        lastName:
          type: string
          minLength: 1
          maxLength: 100
        email:
          allOf:
            - $ref: "#/components/schemas/Email"
          nullable: true
        phone:
          type: string
          minLength: 1
          maxLength: 64
          nullable: true
        matriculationNumber:
          type: string
          minLength: 1
          maxLength: 64
          nullable: true
          description: Omitted unless the caller has `people.read.matriculation`; null means no value.
        photoReference:
          type: string
          minLength: 1
          maxLength: 500
          nullable: true
          readOnly: true
          description: Read-only legacy metadata preserved until a managed profile image replaces it.
        profileImage:
          allOf: [{$ref: "#/components/schemas/ProfileImage"}]
          nullable: true
        profileImageRequired:
          type: boolean
          description: True when any assigned Role requires a profile image.
        account:
          allOf:
            - $ref: "#/components/schemas/AccountSummary"
          nullable: true
          description: Omitted without `accounts.read`; null means this person has no account.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          $ref: "#/components/schemas/Version"

    CreatePersonRequest:
      type: object
      additionalProperties: false
      required: [firstName, lastName]
      description: At least one non-null contact method (`email` or `phone`) is required.
      properties:
        firstName:
          type: string
          minLength: 1
          maxLength: 100
        lastName:
          type: string
          minLength: 1
          maxLength: 100
        email:
          allOf:
            - $ref: "#/components/schemas/Email"
          nullable: true
        phone:
          type: string
          minLength: 1
          maxLength: 64
          nullable: true
        matriculationNumber:
          type: string
          minLength: 1
          maxLength: 64
          nullable: true

    ProfileImage:
      type: object
      additionalProperties: false
      required: [fileId, source, downloadUrl]
      properties:
        fileId: {$ref: "#/components/schemas/UUIDv7"}
        source: {type: string, enum: [terminal_capture, self_upload, admin_upload]}
        downloadUrl: {type: string, pattern: "^/api/v1/people/"}

    PersonMakerspaceStatus:
      type: object
      additionalProperties: false
      properties:
        upcomingOpenDayAssignments:
          type: array
          description: Omitted unless the caller may read Open Day assignment identities.
          items:
            $ref: "#/components/schemas/PersonOpenDayAssignmentSummary"
        laborordnungStatus:
          description: Omitted unless the caller may read Lab Rules confirmation requests.
          allOf:
            - $ref: "#/components/schemas/LaborordnungStatus"

    PersonOpenDayAssignmentSummary:
      type: object
      additionalProperties: false
      required: [assignmentId, openDayId, periodId, periodName, startsAt, endsAt, role]
      properties:
        assignmentId: {$ref: "#/components/schemas/UUIDv7"}
        openDayId: {$ref: "#/components/schemas/UUIDv7"}
        periodId: {$ref: "#/components/schemas/UUIDv7"}
        periodName: {type: string, minLength: 1, maxLength: 150}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        role: {$ref: "#/components/schemas/OpenDayRequirementKind"}

    LaborordnungVersion:
      type: object
      additionalProperties: false
      required: [id, status, humanRevision, pdfFileId, sha256, effectiveAt, publishedAt, createdAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        status: {type: string, enum: [draft, published]}
        humanRevision: {type: string, minLength: 1, maxLength: 100}
        pdfFileId: {$ref: "#/components/schemas/UUIDv7"}
        sha256: {type: string, pattern: "^[0-9a-f]{64}$"}
        effectiveAt: {type: string, format: date-time, nullable: true}
        publishedAt: {type: string, format: date-time, nullable: true}
        createdAt: {type: string, format: date-time}
    LaborordnungVersionList:
      type: object
      additionalProperties: false
      required: [items]
      properties: {items: {type: array, items: {$ref: "#/components/schemas/LaborordnungVersion"}}}
    PublishLaborordnungRequest:
      type: object
      additionalProperties: false
      required: [effectiveAt]
      properties: {effectiveAt: {type: string, format: date-time}}
    LaborordnungStatus:
      type: object
      additionalProperties: false
      required: [mode, state, actionRequired, currentVersion, latestConfirmedVersion, requestId]
      properties:
        mode: {type: string, enum: [not_required, warning, blocking]}
        state: {type: string, enum: [not_required, no_published_version, current, outdated]}
        actionRequired: {type: boolean}
        currentVersion: {allOf: [{$ref: "#/components/schemas/LaborordnungVersion"}], nullable: true}
        latestConfirmedVersion: {allOf: [{$ref: "#/components/schemas/LaborordnungVersion"}], nullable: true}
        requestId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
    LaborordnungRequest:
      type: object
      additionalProperties: false
      required: [id, personId, personName, requiredVersion, previousVersion, status, requestedAt, completedAt, physicalDocumentReference, signedDate, archiveNote]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        personId: {$ref: "#/components/schemas/UUIDv7"}
        personName: {type: string, minLength: 1, maxLength: 201}
        requiredVersion: {$ref: "#/components/schemas/LaborordnungVersion"}
        previousVersion: {allOf: [{$ref: "#/components/schemas/LaborordnungVersion"}], nullable: true}
        status: {type: string, enum: [pending, completed, superseded]}
        requestedAt: {type: string, format: date-time}
        completedAt: {type: string, format: date-time, nullable: true}
        physicalDocumentReference: {type: string, maxLength: 255, nullable: true}
        signedDate: {type: string, format: date, nullable: true}
        archiveNote: {type: string, maxLength: 1000, nullable: true}
    LaborordnungRequestList:
      type: object
      additionalProperties: false
      required: [items]
      properties: {items: {type: array, items: {$ref: "#/components/schemas/LaborordnungRequest"}}}
    ConfirmLaborordnungRequest:
      type: object
      additionalProperties: false
      required: [physicalDocumentReference]
      properties:
        physicalDocumentReference: {type: string, minLength: 1, maxLength: 255}
        signedDate: {type: string, format: date, nullable: true}
        archiveNote: {type: string, maxLength: 1000, nullable: true}

    SupervisorDashboard:
      type: object
      additionalProperties: false
      required: [periods, supervisors, totals]
      properties:
        periods: {type: array, items: {$ref: "#/components/schemas/SupervisorPeriod"}}
        supervisors: {type: array, items: {$ref: "#/components/schemas/SupervisorRow"}}
        totals: {$ref: "#/components/schemas/SupervisorTotals"}
    SupervisorPeriod:
      type: object
      additionalProperties: false
      required: [id, name, status, supervisorAssignments]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string}
        status: {type: string, enum: [staffing, published]}
        supervisorAssignments: {type: integer, minimum: 0}
    SupervisorAssignmentCount:
      type: object
      additionalProperties: false
      required: [periodId, count]
      properties:
        periodId: {$ref: "#/components/schemas/UUIDv7"}
        count: {type: integer, minimum: 0}
    SupervisorRow:
      type: object
      additionalProperties: false
      required: [personId, name, hasProfileImage, laborordnungState, assignmentCounts]
      properties:
        personId: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string}
        hasProfileImage: {type: boolean}
        laborordnungState: {type: string, enum: [not_required, no_published_version, current, outdated]}
        assignmentCounts: {type: array, items: {$ref: "#/components/schemas/SupervisorAssignmentCount"}}
    SupervisorTotals:
      type: object
      additionalProperties: false
      required: [supervisors, profileImagesComplete, laborordnungCurrent, laborordnungOutdated]
      properties:
        supervisors: {type: integer, minimum: 0}
        profileImagesComplete: {type: integer, minimum: 0}
        laborordnungCurrent: {type: integer, minimum: 0}
        laborordnungOutdated: {type: integer, minimum: 0}

    OIDCProvider:
      type: object
      additionalProperties: false
      required: [id, slug, displayName, issuer, clientId, enabled, jitEnabled, acrAssuranceMappings, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        slug: {type: string, pattern: "^[a-z0-9][a-z0-9-]{1,62}$"}
        displayName: {type: string, minLength: 1, maxLength: 100}
        issuer: {type: string, format: uri, maxLength: 500}
        clientId: {type: string, minLength: 1, maxLength: 255}
        enabled: {type: boolean}
        jitEnabled: {type: boolean}
        acrAssuranceMappings:
          type: object
          additionalProperties: {$ref: "#/components/schemas/AuthenticationAssurance"}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
    OIDCProviderList:
      type: object
      additionalProperties: false
      required: [items]
      properties: {items: {type: array, items: {$ref: "#/components/schemas/OIDCProvider"}}}
    OIDCLoginProvider:
      type: object
      additionalProperties: false
      required: [slug, displayName]
      properties:
        slug: {type: string}
        displayName: {type: string}
    OIDCLoginProviderList:
      type: object
      additionalProperties: false
      required: [items]
      properties: {items: {type: array, items: {$ref: "#/components/schemas/OIDCLoginProvider"}}}
    CreateOIDCProviderRequest:
      type: object
      additionalProperties: false
      required: [slug, displayName, issuer, clientId, clientSecret, enabled, jitEnabled, acrAssuranceMappings]
      properties:
        slug: {type: string, pattern: "^[a-z0-9][a-z0-9-]{1,62}$"}
        displayName: {type: string, minLength: 1, maxLength: 100}
        issuer: {type: string, format: uri, maxLength: 500}
        clientId: {type: string, minLength: 1, maxLength: 255}
        clientSecret: {type: string, minLength: 1, maxLength: 4096, writeOnly: true}
        enabled: {type: boolean}
        jitEnabled: {type: boolean}
        acrAssuranceMappings: {type: object, additionalProperties: {$ref: "#/components/schemas/AuthenticationAssurance"}}
    UpdateOIDCProviderRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion, displayName, issuer, clientId, enabled, jitEnabled, acrAssuranceMappings]
      properties:
        expectedVersion: {$ref: "#/components/schemas/Version"}
        displayName: {type: string, minLength: 1, maxLength: 100}
        issuer: {type: string, format: uri, maxLength: 500}
        clientId: {type: string, minLength: 1, maxLength: 255}
        clientSecret: {type: string, minLength: 1, maxLength: 4096, writeOnly: true}
        enabled: {type: boolean}
        jitEnabled: {type: boolean}
        acrAssuranceMappings: {type: object, additionalProperties: {$ref: "#/components/schemas/AuthenticationAssurance"}}
    OIDCLinkRequest:
      type: object
      additionalProperties: false
      properties: {currentPassword: {$ref: "#/components/schemas/ExistingPassword"}}
    OIDCFlowStart:
      type: object
      additionalProperties: false
      required: [authorizationUrl]
      properties: {authorizationUrl: {type: string, format: uri}}

    UpdatePersonRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion]
      description: At least one update field must be present and a contact method must remain afterward.
      properties:
        expectedVersion:
          $ref: "#/components/schemas/Version"
        firstName:
          type: string
          minLength: 1
          maxLength: 100
        lastName:
          type: string
          minLength: 1
          maxLength: 100
        email:
          allOf:
            - $ref: "#/components/schemas/Email"
          nullable: true
        phone:
          type: string
          minLength: 1
          maxLength: 64
          nullable: true
        matriculationNumber:
          type: string
          minLength: 1
          maxLength: 64
          nullable: true

    PersonPage:
      type: object
      additionalProperties: false
      required: [items, page, pageSize, total]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Person"
        page:
          type: integer
          minimum: 1
        pageSize:
          type: integer
          minimum: 1
          maximum: 100
        total:
          type: integer
          format: int64
          minimum: 0

    AccountStatus:
      type: string
      enum: [enabled, disabled]

    PasswordStatus:
      type: string
      enum: [not_set, active, reset_required]
      x-enum-varnames: [PasswordStatusNotSet, PasswordStatusActive, PasswordStatusResetRequired]

    AccountSummary:
      type: object
      additionalProperties: false
      required: [id, personId, status, provisioningSource, firstAuthenticatedAt, passwordStatus, loginEmail, authIdentities, roles, version]
      properties:
        id:
          $ref: "#/components/schemas/UUIDv7"
        personId:
          $ref: "#/components/schemas/UUIDv7"
        status:
          $ref: "#/components/schemas/AccountStatus"
        passwordStatus:
          $ref: "#/components/schemas/PasswordStatus"
        loginEmail:
          allOf: [{$ref: "#/components/schemas/Email"}]
          nullable: true
          deprecated: true
          description: Compatibility view of the local password identifier; use authIdentities.
        provisioningSource:
          type: string
          enum: [local, invitation, oidc_jit, scim, visitor]
        firstAuthenticatedAt:
          type: string
          format: date-time
          nullable: true
        authIdentities:
          type: array
          items: {$ref: "#/components/schemas/AuthIdentitySummary"}
        roles:
          type: array
          items:
            $ref: "#/components/schemas/RoleSummary"
        version:
          $ref: "#/components/schemas/Version"

    Account:
      type: object
      additionalProperties: false
      required: [id, personId, status, provisioningSource, firstAuthenticatedAt, passwordStatus, loginEmail, authIdentities, roles, createdAt, updatedAt, version]
      properties:
        id:
          $ref: "#/components/schemas/UUIDv7"
        personId:
          $ref: "#/components/schemas/UUIDv7"
        status:
          $ref: "#/components/schemas/AccountStatus"
        passwordStatus:
          $ref: "#/components/schemas/PasswordStatus"
        loginEmail:
          allOf: [{$ref: "#/components/schemas/Email"}]
          nullable: true
          deprecated: true
        provisioningSource:
          type: string
          enum: [local, invitation, oidc_jit, scim, visitor]
        firstAuthenticatedAt:
          type: string
          format: date-time
          nullable: true
        authIdentities:
          type: array
          items: {$ref: "#/components/schemas/AuthIdentitySummary"}
        roles:
          type: array
          items:
            $ref: "#/components/schemas/RoleSummary"
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          $ref: "#/components/schemas/Version"

    CreateAccountRequest:
      type: object
      additionalProperties: false
      required: [loginEmail, expectedVersion]
      properties:
        loginEmail:
          allOf: [{$ref: "#/components/schemas/Email"}]
          nullable: true
        expectedVersion:
          allOf:
            - $ref: "#/components/schemas/Version"
          description: Expected version of the owning person loaded by the caller.

    AuthIdentitySummary:
      type: object
      additionalProperties: false
      required: [id, kind, displayIdentifier, verifiedAt, disabledAt, createdAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        kind: {type: string, enum: [password, pin, oidc]}
        providerSlug: {type: string, description: OIDC provider used for self-service reauthentication; omitted for local identities.}
        displayIdentifier: {type: string, nullable: true, maxLength: 254}
        verifiedAt: {type: string, format: date-time, nullable: true}
        disabledAt: {type: string, format: date-time, nullable: true}
        createdAt: {type: string, format: date-time}

    UpdateLoginEmailRequest:
      type: object
      additionalProperties: false
      required: [loginEmail, expectedVersion]
      properties:
        loginEmail:
          $ref: "#/components/schemas/Email"
        expectedVersion:
          $ref: "#/components/schemas/Version"

    SetAccountPasswordRequest:
      type: object
      additionalProperties: false
      required: [newPassword, expectedVersion]
      properties:
        newPassword:
          $ref: "#/components/schemas/NewPassword"
        expectedVersion:
          $ref: "#/components/schemas/Version"

    PasswordResetIssue:
      type: object
      additionalProperties: false
      required: [expiresAt, account, deliveryStatus, setupUrl]
      properties:
        expiresAt:
          type: string
          format: date-time
        account:
          $ref: "#/components/schemas/Account"
        deliveryStatus:
          type: string
          enum: [sent, manual]
        setupUrl:
          type: string
          nullable: true
          description: One-time relative setup URL returned only to an authorized administrator when mail is not configured.

    MailConfiguration:
      type: object
      additionalProperties: false
      required: [enabled, provider, host, port, tlsMode, username, passwordConfigured, fromAddress, fromName, baseUrl, version, updatedAt]
      properties:
        enabled: {type: boolean}
        provider: {type: string, enum: [smtp]}
        host: {type: string}
        port: {type: integer, minimum: 1, maximum: 65535}
        tlsMode: {type: string, enum: [starttls, tls, none]}
        username: {type: string}
        passwordConfigured: {type: boolean}
        fromAddress: {type: string}
        fromName: {type: string}
        baseUrl: {type: string}
        version: {$ref: "#/components/schemas/Version"}
        updatedAt: {type: string, format: date-time}

    UpdateMailConfigurationRequest:
      type: object
      additionalProperties: false
      required: [enabled, host, port, tlsMode, username, password, fromAddress, fromName, baseUrl, expectedVersion]
      properties:
        enabled: {type: boolean}
        host: {type: string, maxLength: 255}
        port: {type: integer, minimum: 1, maximum: 65535}
        tlsMode: {type: string, enum: [starttls, tls, none]}
        username: {type: string, maxLength: 255}
        password: {type: string, maxLength: 1024, writeOnly: true, description: Leave empty to retain the existing encrypted password.}
        fromAddress: {type: string, maxLength: 320}
        fromName: {type: string, maxLength: 255}
        baseUrl: {type: string, maxLength: 2048}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    VersionRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion]
      properties:
        expectedVersion:
          $ref: "#/components/schemas/Version"

    Permission:
      type: object
      additionalProperties: false
      required: [id, description]
      properties:
        id:
          $ref: "#/components/schemas/PermissionId"
        description:
          type: string
          minLength: 1
          maxLength: 500

    ManagedDeviceType:
      type: object
      additionalProperties: false
      required: [id, name, description, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string, minLength: 1, maxLength: 100}
        description: {type: string, maxLength: 500, nullable: true}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
    ManagedDeviceTypeList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, maxItems: 500, items: {$ref: "#/components/schemas/ManagedDeviceType"}}
    CreateManagedDeviceTypeRequest:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: {type: string, minLength: 1, maxLength: 100}
        description: {type: string, maxLength: 500, nullable: true}
    UpdateManagedDeviceTypeRequest:
      type: object
      additionalProperties: false
      required: [name, description, expectedVersion]
      properties:
        name: {type: string, minLength: 1, maxLength: 100}
        description: {type: string, maxLength: 500, nullable: true}
        expectedVersion: {$ref: "#/components/schemas/Version"}
    ManagedDeviceStatus:
      type: string
      enum: [active, expired, revoked]
    ManagedDevice:
      type: object
      additionalProperties: false
      required: [id, name, deviceTypeId, deviceTypeName, status, expiresAt, revokedAt, lastSeenAt, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string, minLength: 1, maxLength: 100}
        deviceTypeId: {$ref: "#/components/schemas/UUIDv7"}
        deviceTypeName: {type: string, minLength: 1, maxLength: 100}
        status: {$ref: "#/components/schemas/ManagedDeviceStatus"}
        expiresAt: {type: string, format: date-time, nullable: true}
        revokedAt: {type: string, format: date-time, nullable: true}
        lastSeenAt: {type: string, format: date-time, nullable: true}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}
    ManagedDeviceList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/ManagedDevice"}}
        nextCursor:
          type: string
          maxLength: 500
          nullable: true
    CreateManagedDeviceRequest:
      type: object
      additionalProperties: false
      required: [name, deviceTypeId, expiresAt, credentialDelivery]
      properties:
        name: {type: string, minLength: 1, maxLength: 100}
        deviceTypeId: {$ref: "#/components/schemas/UUIDv7"}
        expiresAt: {type: string, format: date-time, nullable: true}
        credentialDelivery: {$ref: "#/components/schemas/ManagedDeviceCredentialDelivery"}
    UpdateManagedDeviceRequest:
      type: object
      additionalProperties: false
      required: [name, deviceTypeId, expiresAt, expectedVersion]
      properties:
        name: {type: string, minLength: 1, maxLength: 100}
        deviceTypeId: {$ref: "#/components/schemas/UUIDv7"}
        expiresAt: {type: string, format: date-time, nullable: true}
        expectedVersion: {$ref: "#/components/schemas/Version"}
    RotateManagedDeviceTokenRequest:
      type: object
      additionalProperties: false
      required: [expiresAt, expectedVersion, credentialDelivery]
      properties:
        expiresAt: {type: string, format: date-time, nullable: true}
        expectedVersion: {$ref: "#/components/schemas/Version"}
        credentialDelivery: {$ref: "#/components/schemas/ManagedDeviceCredentialDelivery"}
    ManagedDeviceCredentialDelivery:
      type: string
      enum: [nativeToken, bindBrowser]
    ManagedDeviceProvisioning:
      type: object
      additionalProperties: false
      required: [device]
      properties:
        device: {$ref: "#/components/schemas/ManagedDevice"}
        token:
          type: string
          minLength: 43
          maxLength: 43
          readOnly: true
          description: Returned only by device creation and token rotation; it cannot be retrieved later.

    RoleSummary:
      type: object
      additionalProperties: false
      required: [id, name]
      properties:
        id:
          $ref: "#/components/schemas/UUIDv7"
        name:
          type: string
          minLength: 1
          maxLength: 100
        systemKey:
          type: string
          enum: [master]
          nullable: true
          description: Present only for a protected system role.

    Role:
      type: object
      additionalProperties: false
      required: [id, name, description, systemKey, permissionGrants, profileImageRequired, laborordnungMode, supervisorDashboard, createdAt, updatedAt, version]
      properties:
        id:
          $ref: "#/components/schemas/UUIDv7"
        name:
          type: string
          minLength: 1
          maxLength: 100
        description:
          type: string
          maxLength: 500
          nullable: true
        systemKey:
          type: string
          enum: [master]
          nullable: true
        permissionGrants:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/PermissionGrant"
          description: For master, this is the current complete application permission set.
        profileImageRequired:
          type: boolean
        laborordnungMode:
          type: string
          enum: [not_required, warning, blocking]
        supervisorDashboard:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          $ref: "#/components/schemas/Version"

    CreateRoleRequest:
      type: object
      additionalProperties: false
      required: [name, permissionGrants]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
        description:
          type: string
          maxLength: 500
          nullable: true
        permissionGrants:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/PermissionGrant"
        profileImageRequired:
          type: boolean
          default: false
        laborordnungMode:
          type: string
          enum: [not_required, warning, blocking]
          default: not_required
        supervisorDashboard:
          type: boolean
          default: false

    UpdateRoleRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion]
      description: At least one update field must be present.
      properties:
        profileImageRequired:
          type: boolean
        laborordnungMode:
          type: string
          enum: [not_required, warning, blocking]
        supervisorDashboard:
          type: boolean
        expectedVersion:
          $ref: "#/components/schemas/Version"
        name:
          type: string
          minLength: 1
          maxLength: 100
        description:
          type: string
          maxLength: 500
          nullable: true

    ReplaceRolePermissionsRequest:
      type: object
      additionalProperties: false
      required: [permissionGrants, expectedVersion]
      properties:
        permissionGrants:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/PermissionGrant"
        expectedVersion:
          $ref: "#/components/schemas/Version"

    PermissionGrant:
      type: object
      additionalProperties: false
      required: [permissionId, scope, deviceTypeIds, minimumAssurance]
      properties:
        id:
          allOf: [{$ref: "#/components/schemas/UUIDv7"}]
          readOnly: true
        permissionId: {$ref: "#/components/schemas/PermissionId"}
        scope:
          type: string
          enum: [everywhere, anyManagedDevice, selectedDeviceTypes]
        deviceTypeIds:
          type: array
          uniqueItems: true
          items: {$ref: "#/components/schemas/UUIDv7"}
        minimumAssurance:
          $ref: "#/components/schemas/AuthenticationAssurance"

    AuthenticationAssurance:
      type: string
      enum: [low, normal, strong, strong_mfa]
      description: Ordered authentication assurance required by a permission grant or held by a session.

    RoleEffectivePermissionEvaluation:
      type: object
      additionalProperties: false
      required: [roleId, roleVersion, permissionIds]
      properties:
        roleId:
          $ref: "#/components/schemas/UUIDv7"
        roleVersion:
          $ref: "#/components/schemas/Version"
        permissionIds:
          type: array
          uniqueItems: true
          items:
            $ref: "#/components/schemas/PermissionId"

    RoleEffectivePermissionEvaluationList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/RoleEffectivePermissionEvaluation"

    RolePage:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Role"
        nextCursor:
          type: string
          maxLength: 500
          nullable: true

    AuditEvent:
      type: object
      additionalProperties: false
      required: [id, actorType, action, resourceType, occurredAt, requestId, changedFields, metadata, resolvedMetadata, source]
      properties:
        id:
          $ref: "#/components/schemas/UUIDv7"
        actorAccountId:
          allOf:
            - $ref: "#/components/schemas/UUIDv7"
          nullable: true
          description: Null for system/CLI actions or after privacy-preserving deletion.
        actorType:
          $ref: "#/components/schemas/AuditActorType"
        actorDisplayName:
          type: string
          maxLength: 300
          nullable: true
          description: Current minimal display name; never a retained historical snapshot.
        action:
          type: string
          minLength: 1
          maxLength: 128
        resourceType:
          type: string
          minLength: 1
          maxLength: 64
        resourceId:
          allOf:
            - $ref: "#/components/schemas/UUIDv7"
          nullable: true
        resourceDisplayName:
          type: string
          maxLength: 300
          nullable: true
          description: Current minimal resource label; null after deletion or when no safe label exists.
        occurredAt:
          type: string
          format: date-time
        requestId:
          type: string
          minLength: 1
          maxLength: 200
          nullable: true
          description: Null for system or administrative CLI events without an HTTP request.
        changedFields:
          type: array
          uniqueItems: true
          items:
            type: string
            maxLength: 100
          description: Field names only; never before/after values.
        metadata:
          type: object
          additionalProperties:
            type: string
            maxLength: 500
          description: Selected non-sensitive values only.
        resolvedMetadata:
          type: object
          readOnly: true
          additionalProperties:
            type: string
            maxLength: 500
          description: Current display labels for allowlisted opaque metadata identifiers; never stored snapshots.
        source:
          type: string
          enum: [http, admin_cli, system]

    AuditActorType:
      type: string
      enum: [user, system, unknown]

    AuditEventPage:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AuditEvent"
        nextCursor:
          type: string
          maxLength: 500
          nullable: true

    OpenDayPeriodStatus:
      type: string
      enum: [draft, staffing, published, archived]

    OpenDayStatus:
      type: string
      enum: [scheduled, cancelled]

    OpenDayRequirementKind:
      type: string
      enum: [supervisor, trainee]

    OpenDayPeriod:
      type: object
      additionalProperties: false
      required: [id, name, startsOn, endsOn, status, totalOpenDays, fullyStaffedCount, needsStaffCount, openSupervisorPositions, cancelledCount, myAssignmentCount, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string, maxLength: 150}
        startsOn: {type: string, format: date}
        endsOn: {type: string, format: date}
        status: {$ref: "#/components/schemas/OpenDayPeriodStatus"}
        totalOpenDays: {type: integer, minimum: 0}
        fullyStaffedCount: {type: integer, minimum: 0}
        needsStaffCount: {type: integer, minimum: 0}
        openSupervisorPositions:
          type: integer
          minimum: 0
          description: Total unfilled positions across scheduled supervisor requirements; trainee vacancies are excluded.
        cancelledCount: {type: integer, minimum: 0}
        myAssignmentCount: {type: integer, minimum: 0}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    OpenDayPeriodList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/OpenDayPeriod"}}

    CreateOpenDayPeriodRequest:
      type: object
      additionalProperties: false
      required: [name, startsOn, endsOn]
      properties:
        name: {type: string, minLength: 1, maxLength: 150}
        startsOn: {type: string, format: date}
        endsOn: {type: string, format: date}

    UpdateOpenDayPeriodRequest:
      type: object
      additionalProperties: false
      required: [name, startsOn, endsOn, expectedVersion]
      properties:
        name: {type: string, minLength: 1, maxLength: 150}
        startsOn: {type: string, format: date}
        endsOn: {type: string, format: date}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    StaffingRequirementInput:
      type: object
      additionalProperties: false
      required: [kind, requiredCount, eligibleRoleIds]
      properties:
        kind: {$ref: "#/components/schemas/OpenDayRequirementKind"}
        requiredCount: {type: integer, minimum: 0, maximum: 100}
        eligibleRoleIds:
          type: array
          uniqueItems: true
          items: {$ref: "#/components/schemas/UUIDv7"}

    CreateOpenDayRequest:
      type: object
      additionalProperties: false
      required: [startsAt, endsAt, requirements, expectedPeriodVersion]
      properties:
        startsAt: {type: string, format: date-time, description: Unambiguous instant. Scheduling rejects ambiguous local endpoints; send UTC or the correct offset for the configured makerspace timezone.}
        endsAt: {type: string, format: date-time}
        internalNote: {type: string, maxLength: 2000, nullable: true}
        requirements:
          type: array
          minItems: 2
          maxItems: 2
          items: {$ref: "#/components/schemas/StaffingRequirementInput"}
        expectedPeriodVersion: {$ref: "#/components/schemas/Version"}

    UpdateOpenDayRequest:
      allOf:
        - {$ref: "#/components/schemas/CreateOpenDayRequest"}
        - type: object
          required: [expectedVersion]
          properties:
            expectedVersion: {$ref: "#/components/schemas/Version"}

    MinimalPerson:
      type: object
      additionalProperties: false
      required: [personId, displayName]
      properties:
        personId: {$ref: "#/components/schemas/UUIDv7"}
        displayName: {type: string, maxLength: 201}

    OpenDayAssignment:
      type: object
      additionalProperties: false
      required: [id, openDayId, requirementId, personId, displayName, isCurrentUser, createdAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        openDayId: {$ref: "#/components/schemas/UUIDv7"}
        requirementId: {$ref: "#/components/schemas/UUIDv7"}
        personId: {$ref: "#/components/schemas/UUIDv7"}
        displayName: {type: string, maxLength: 201}
        isCurrentUser: {type: boolean}
        createdAt: {type: string, format: date-time}

    OpenDayStaffRequirement:
      type: object
      additionalProperties: false
      required: [id, kind, requiredCount, assignedCount]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        kind: {$ref: "#/components/schemas/OpenDayRequirementKind"}
        requiredCount: {type: integer, minimum: 0, maximum: 100}
        assignedCount: {type: integer, minimum: 0}
        eligibleRoleIds:
          type: array
          description: Omitted unless the caller has open_days.manage.
          items: {$ref: "#/components/schemas/UUIDv7"}
        assignments:
          type: array
          description: Omitted unless other assignment identities are authorized.
          items: {$ref: "#/components/schemas/OpenDayAssignment"}

    OpenDay:
      type: object
      additionalProperties: false
      required: [id, periodId, startsAt, endsAt, status, requirements, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        periodId: {$ref: "#/components/schemas/UUIDv7"}
        startsAt: {type: string, format: date-time, description: Unambiguous instant. Scheduling rejects ambiguous local endpoints; send UTC or the correct offset for the configured makerspace timezone.}
        endsAt: {type: string, format: date-time}
        internalNote:
          type: string
          maxLength: 2000
          nullable: true
          description: Omitted unless the caller has open_days.manage.
        status: {$ref: "#/components/schemas/OpenDayStatus"}
        requirements: {type: array, items: {$ref: "#/components/schemas/OpenDayStaffRequirement"}}
        myAssignment:
          allOf: [{$ref: "#/components/schemas/OpenDayAssignment"}]
          nullable: true
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    OpenDaySchedule:
      type: object
      additionalProperties: false
      required: [period, items, timeZone]
      properties:
        period: {$ref: "#/components/schemas/OpenDayPeriod"}
        items: {type: array, items: {$ref: "#/components/schemas/OpenDay"}}
        timeZone: {type: string, maxLength: 100}

    ScheduleCreate:
      type: object
      additionalProperties: false
      required: [startsAt, endsAt, requirements]
      properties:
        startsAt: {type: string, format: date-time, description: Unambiguous instant. Scheduling rejects ambiguous local endpoints; send UTC or the correct offset for the configured makerspace timezone.}
        endsAt: {type: string, format: date-time}
        internalNote: {type: string, maxLength: 2000, nullable: true}
        requirements:
          type: array
          minItems: 2
          maxItems: 2
          items: {$ref: "#/components/schemas/StaffingRequirementInput"}

    ScheduleUpdate:
      type: object
      additionalProperties: false
      required: [id, startsAt, endsAt, requirements, expectedVersion]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        startsAt: {type: string, format: date-time, description: Unambiguous instant. Scheduling rejects ambiguous local endpoints; send UTC or the correct offset for the configured makerspace timezone.}
        endsAt: {type: string, format: date-time}
        internalNote: {type: string, maxLength: 2000, nullable: true}
        requirements:
          type: array
          minItems: 2
          maxItems: 2
          items: {$ref: "#/components/schemas/StaffingRequirementInput"}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    ScheduleRemoval:
      type: object
      additionalProperties: false
      required: [id, expectedVersion]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    SaveOpenDayScheduleRequest:
      type: object
      additionalProperties: false
      required: [expectedPeriodVersion, creates, updates, removals]
      properties:
        expectedPeriodVersion: {$ref: "#/components/schemas/Version"}
        creates: {type: array, maxItems: 366, items: {$ref: "#/components/schemas/ScheduleCreate"}}
        updates: {type: array, maxItems: 366, items: {$ref: "#/components/schemas/ScheduleUpdate"}}
        removals: {type: array, maxItems: 366, items: {$ref: "#/components/schemas/ScheduleRemoval"}}

    RecurrencePreviewRequest:
      type: object
      additionalProperties: false
      required: [weekday, startsOn, endsOn, startTime, endTime, everyWeeks, skipPublicHolidays, skipAcademicBreaks]
      properties:
        weekday: {type: integer, minimum: 1, maximum: 7, description: "ISO weekday; Monday is 1."}
        startsOn: {type: string, format: date}
        endsOn: {type: string, format: date}
        startTime: {type: string, description: Local wall time; daylight-saving gaps and overlaps return validation errors., pattern: "^([01][0-9]|2[0-3]):[0-5][0-9]$"}
        endTime: {type: string, pattern: "^([01][0-9]|2[0-3]):[0-5][0-9]$"}
        everyWeeks: {type: integer, minimum: 1, maximum: 52}
        skipPublicHolidays: {type: boolean}
        skipAcademicBreaks: {type: boolean}

    RecurrenceOccurrence:
      type: object
      additionalProperties: false
      required: [date, startsAt, endsAt, disposition]
      properties:
        date: {type: string, format: date}
        startsAt: {type: string, format: date-time, description: Unambiguous instant. Scheduling rejects ambiguous local endpoints; send UTC or the correct offset for the configured makerspace timezone.}
        endsAt: {type: string, format: date-time}
        disposition: {type: string, enum: [create, publicHoliday, academicBreak, conflict]}
        reason: {type: string, maxLength: 200}

    RecurrencePreview:
      type: object
      additionalProperties: false
      required: [occurrences]
      properties:
        occurrences: {type: array, items: {$ref: "#/components/schemas/RecurrenceOccurrence"}}

    JoinOpenDayRequest:
      type: object
      additionalProperties: false
      required: [requirementId]
      properties:
        requirementId: {$ref: "#/components/schemas/UUIDv7"}

    AssignOpenDayPersonRequest:
      type: object
      additionalProperties: false
      required: [requirementId, personId]
      properties:
        requirementId: {$ref: "#/components/schemas/UUIDv7"}
        personId: {$ref: "#/components/schemas/UUIDv7"}

    EligiblePersonList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/MinimalPerson"}}

    EligibilityRole:
      type: object
      additionalProperties: false
      required: [id, name]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string, maxLength: 100}

    EligibilityRoleList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/EligibilityRole"}}

    CalendarEntry:
      type: object
      additionalProperties: false
      required: [name, startsOn, endsOn, category, source]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string, maxLength: 150}
        startsOn: {type: string, format: date}
        endsOn: {type: string, format: date}
        category: {type: string, enum: [publicHoliday, academicBreak]}
        source: {type: string, enum: [holidayLibrary, manual]}

    AcademicBreak:
      type: object
      additionalProperties: false
      required: [id, name, startsOn, endsOn, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string, maxLength: 150}
        startsOn: {type: string, format: date}
        endsOn: {type: string, format: date}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    CreateAcademicBreakRequest:
      type: object
      additionalProperties: false
      required: [name, startsOn, endsOn]
      properties:
        name: {type: string, minLength: 1, maxLength: 150}
        startsOn: {type: string, format: date}
        endsOn: {type: string, format: date}

    UpdateAcademicBreakRequest:
      allOf:
        - {$ref: "#/components/schemas/CreateAcademicBreakRequest"}
        - type: object
          required: [expectedVersion]
          properties:
            expectedVersion: {$ref: "#/components/schemas/Version"}

    OpenDayCalendarContext:
      type: object
      additionalProperties: false
      required: [timeZone, countryCode, subdivisionCode, languageCode, entries, academicBreaks]
      properties:
        timeZone: {type: string, maxLength: 100}
        countryCode: {type: string, minLength: 2, maxLength: 2}
        subdivisionCode: {type: string, maxLength: 20, nullable: true}
        languageCode: {type: string, maxLength: 10}
        entries: {type: array, items: {$ref: "#/components/schemas/CalendarEntry"}}
        academicBreaks: {type: array, items: {$ref: "#/components/schemas/AcademicBreak"}}

    PublicOpenDay:
      type: object
      additionalProperties: false
      required: [id, title, startsAt, endsAt, status, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        title: {type: string, enum: [Open Day]}
        startsAt: {type: string, format: date-time, description: Unambiguous instant. Scheduling rejects ambiguous local endpoints; send UTC or the correct offset for the configured makerspace timezone.}
        endsAt: {type: string, format: date-time}
        status: {$ref: "#/components/schemas/OpenDayStatus"}
        updatedAt: {type: string, format: date-time}

    PublicOpenDayList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/PublicOpenDay"}}

    VisitorAuthenticationMethod:
      type: string
      enum: [password, pin]

    VisitorEnrollmentConfiguration:
      type: object
      additionalProperties: false
      required: [enabled, initialRoleId, deviceTypeIds, allowedMethods, version, updatedAt]
      properties:
        enabled: {type: boolean}
        initialRoleId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        deviceTypeIds: {type: array, uniqueItems: true, items: {$ref: "#/components/schemas/UUIDv7"}}
        allowedMethods: {type: array, uniqueItems: true, items: {$ref: "#/components/schemas/VisitorAuthenticationMethod"}}
        version: {$ref: "#/components/schemas/Version"}
        updatedAt: {type: string, format: date-time}

    UpdateVisitorEnrollmentConfigurationRequest:
      type: object
      additionalProperties: false
      required: [enabled, initialRoleId, deviceTypeIds, allowedMethods, expectedVersion]
      properties:
        enabled: {type: boolean}
        initialRoleId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        deviceTypeIds: {type: array, uniqueItems: true, items: {$ref: "#/components/schemas/UUIDv7"}}
        allowedMethods: {type: array, uniqueItems: true, items: {$ref: "#/components/schemas/VisitorAuthenticationMethod"}}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    VisitorLabRulesVersion:
      type: object
      additionalProperties: false
      required: [id, humanRevision, effectiveAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        humanRevision: {type: string, minLength: 1, maxLength: 100}
        effectiveAt: {type: string, format: date-time}

    VisitorEnrollmentState:
      type: object
      additionalProperties: false
      required: [allowedMethods, currentLabRules, expiresAt]
      properties:
        allowedMethods: {type: array, uniqueItems: true, items: {$ref: "#/components/schemas/VisitorAuthenticationMethod"}}
        currentLabRules: {allOf: [{$ref: "#/components/schemas/VisitorLabRulesVersion"}], nullable: true}
        expiresAt: {type: string, format: date-time}

    VisitorEnrollmentSubmission:
      type: object
      additionalProperties: false
      required: [firstName, lastName, email, phone, authMethods, pinLoginName, pin, profileImageBase64, profileImageFilename, requestConfirmation]
      properties:
        firstName: {type: string, minLength: 1, maxLength: 100}
        lastName: {type: string, minLength: 1, maxLength: 100}
        email: {type: string, format: email, nullable: true}
        phone: {type: string, minLength: 1, maxLength: 64, nullable: true}
        authMethods: {type: array, minItems: 1, uniqueItems: true, items: {$ref: "#/components/schemas/VisitorAuthenticationMethod"}}
        pinLoginName: {type: string, minLength: 3, maxLength: 64, nullable: true}
        pin: {type: string, pattern: "^[0-9]{6,12}$", writeOnly: true, nullable: true}
        profileImageBase64: {type: string, format: byte, minLength: 4, maxLength: 12000000, writeOnly: true}
        profileImageFilename: {type: string, minLength: 1, maxLength: 255}
        requestConfirmation: {type: boolean, description: Must be true as the visitor's explicit request to enter the physical-signature workflow.}

    VisitorEnrollmentResult:
      type: object
      additionalProperties: false
      required: [personId, accountId, accountStatus, invitationDelivery, labRulesRequestId, admission]
      properties:
        personId: {$ref: "#/components/schemas/UUIDv7"}
        accountId: {$ref: "#/components/schemas/UUIDv7"}
        accountStatus: {type: string, enum: [enabled, disabled]}
        invitationDelivery: {type: string, enum: [sent, failed], nullable: true}
        labRulesRequestId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        admission: {type: string, enum: [admitted, warning, blocked]}

    VisitorAdmissionResult:
      type: object
      additionalProperties: false
      required: [decision, status, requestId]
      properties:
        decision: {type: string, enum: [admitted, warning, blocked]}
        status: {$ref: "#/components/schemas/LaborordnungStatus"}
        requestId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}

    SCIMConnector:
      type: object
      additionalProperties: false
      required: [id, name, oidcProviderId, enabled, tokenExpiresAt, tokenRevokedAt, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string, minLength: 1, maxLength: 100}
        oidcProviderId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        enabled: {type: boolean}
        tokenExpiresAt: {type: string, format: date-time, nullable: true}
        tokenRevokedAt: {type: string, format: date-time, nullable: true}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    SCIMConnectorList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/SCIMConnector"}}

    CreateSCIMConnectorRequest:
      type: object
      additionalProperties: false
      required: [name, oidcProviderId, enabled, tokenExpiresAt]
      properties:
        name: {type: string, minLength: 1, maxLength: 100}
        oidcProviderId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        enabled: {type: boolean}
        tokenExpiresAt: {type: string, format: date-time}

    UpdateSCIMConnectorRequest:
      type: object
      additionalProperties: false
      required: [name, oidcProviderId, enabled, expectedVersion]
      properties:
        name: {type: string, minLength: 1, maxLength: 100}
        oidcProviderId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        enabled: {type: boolean}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    RotateSCIMTokenRequest:
      type: object
      additionalProperties: false
      required: [expiresAt, expectedVersion]
      properties:
        expiresAt: {type: string, format: date-time}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    SCIMConnectorTokenIssue:
      type: object
      additionalProperties: false
      required: [connector, bearerToken]
      properties:
        connector: {$ref: "#/components/schemas/SCIMConnector"}
        bearerToken: {type: string, minLength: 32, maxLength: 512, writeOnly: true}

    SCIMReconciliationRequest:
      type: object
      additionalProperties: false
      required: [provisionalAccountId, targetAccountId]
      properties:
        provisionalAccountId: {$ref: "#/components/schemas/UUIDv7"}
        targetAccountId: {$ref: "#/components/schemas/UUIDv7"}

    SCIMReconciliationConflict:
      type: object
      additionalProperties: false
      required: [code, resourceId, message]
      properties:
        code: {type: string, maxLength: 64}
        resourceId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        message: {type: string, maxLength: 500}

    SCIMReconciliationReport:
      type: object
      additionalProperties: false
      required: [canReconcile, provisionalAccountId, targetAccountId, conflicts, completed]
      properties:
        canReconcile: {type: boolean}
        provisionalAccountId: {$ref: "#/components/schemas/UUIDv7"}
        targetAccountId: {$ref: "#/components/schemas/UUIDv7"}
        conflicts: {type: array, items: {$ref: "#/components/schemas/SCIMReconciliationConflict"}}
        completed: {type: boolean}

    SCIMName:
      type: object
      additionalProperties: false
      required: [givenName, familyName]
      properties:
        givenName: {type: string, minLength: 1, maxLength: 100}
        familyName: {type: string, minLength: 1, maxLength: 100}
        formatted: {type: string, maxLength: 201}

    SCIMMultiValue:
      type: object
      additionalProperties: true
      required: [value]
      properties:
        value: {type: string, minLength: 1, maxLength: 254}
        type: {type: string, maxLength: 32}
        primary: {type: boolean}

    SCIMMeta:
      type: object
      additionalProperties: false
      required: [resourceType, created, lastModified, location, version]
      properties:
        resourceType: {type: string, enum: [User]}
        created: {type: string, format: date-time}
        lastModified: {type: string, format: date-time}
        location: {type: string, maxLength: 500}
        version: {type: string, maxLength: 100}

    SCIMUserWrite:
      type: object
      additionalProperties: false
      required: [userName, name, active]
      properties:
        schemas: {type: array, items: {type: string}, maxItems: 5}
        userName: {type: string, minLength: 1, maxLength: 255}
        externalId: {type: string, minLength: 1, maxLength: 255, nullable: true, description: Immutable OIDC subject for a provider-bound connector. Attempts to change it return SCIM 409 mutability; ordinary attributes remain editable.}
        active: {type: boolean}
        name: {$ref: "#/components/schemas/SCIMName"}
        emails: {type: array, maxItems: 10, items: {$ref: "#/components/schemas/SCIMMultiValue"}}
        phoneNumbers: {type: array, maxItems: 10, items: {$ref: "#/components/schemas/SCIMMultiValue"}}

    SCIMUser:
      allOf:
        - $ref: "#/components/schemas/SCIMUserWrite"
        - type: object
          additionalProperties: false
          required: [id, schemas, meta]
          properties:
            id: {$ref: "#/components/schemas/UUIDv7"}
            schemas: {type: array, items: {type: string}}
            meta: {$ref: "#/components/schemas/SCIMMeta"}

    SCIMUserListResponse:
      type: object
      additionalProperties: false
      required: [schemas, totalResults, startIndex, itemsPerPage, Resources]
      properties:
        schemas: {type: array, items: {type: string}}
        totalResults: {type: integer, minimum: 0}
        startIndex: {type: integer, minimum: 1}
        itemsPerPage: {type: integer, minimum: 0}
        Resources: {type: array, items: {$ref: "#/components/schemas/SCIMUser"}}

    SCIMPatchOperation:
      type: object
      additionalProperties: false
      required: [op]
      properties:
        op: {type: string, enum: [add, replace, remove]}
        path: {type: string, maxLength: 255}
        value: {}

    SCIMPatchRequest:
      type: object
      additionalProperties: false
      required: [schemas, Operations]
      properties:
        schemas: {type: array, minItems: 1, maxItems: 5, items: {type: string}}
        Operations: {type: array, minItems: 1, maxItems: 50, items: {$ref: "#/components/schemas/SCIMPatchOperation"}}

    SCIMError:
      type: object
      additionalProperties: false
      required: [schemas, status, detail]
      properties:
        schemas: {type: array, items: {type: string}}
        status: {type: string, pattern: "^[1-5][0-9]{2}$"}
        scimType: {type: string, maxLength: 64}
        detail: {type: string, maxLength: 500}

    SCIMServiceProviderConfig:
      type: object
      additionalProperties: false
      required: [schemas, patch, bulk, filter, changePassword, sort, etag]
      properties:
        schemas: {type: array, items: {type: string}}
        patch: {type: object, additionalProperties: false, required: [supported], properties: {supported: {type: boolean}}}
        bulk: {type: object, additionalProperties: false, required: [supported], properties: {supported: {type: boolean}}}
        filter: {type: object, additionalProperties: false, required: [supported, maxResults], properties: {supported: {type: boolean}, maxResults: {type: integer}}}
        changePassword: {type: object, additionalProperties: false, required: [supported], properties: {supported: {type: boolean}}}
        sort: {type: object, additionalProperties: false, required: [supported], properties: {supported: {type: boolean}}}
        etag: {type: object, additionalProperties: false, required: [supported], properties: {supported: {type: boolean}}}

    SCIMListResponse:
      type: object
      additionalProperties: false
      required: [schemas, totalResults, startIndex, itemsPerPage, Resources]
      properties:
        schemas: {type: array, items: {type: string}}
        totalResults: {type: integer, minimum: 0}
        startIndex: {type: integer, minimum: 1}
        itemsPerPage: {type: integer, minimum: 0}
        Resources: {type: array, items: {type: object, additionalProperties: true}}

    Decimal:
      type: string
      pattern: "^-?(0|[1-9][0-9]*)(\\.[0-9]{1,6})?$"
      example: "14.070000"

    Money:
      type: string
      pattern: "^(0|[1-9][0-9]*)(\\.[0-9]{1,2})?$"
      example: "14.07"

    MaterialUnit:
      type: string
      enum: [g, m, ml, m2, piece]

    MachineStatus:
      type: string
      enum: [active, maintenance, retired]

    MachineType:
      type: object
      additionalProperties: false
      required: [id, name, active, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string}
        active: {type: boolean}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    MachineTypeList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/MachineType"}}

    CreateMachineTypeRequest:
      type: object
      additionalProperties: false
      required: [name]
      properties:
        name: {type: string, minLength: 1, maxLength: 120}

    UpdateMachineTypeRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion]
      properties:
        expectedVersion: {$ref: "#/components/schemas/Version"}
        name: {type: string, minLength: 1, maxLength: 120}
        active: {type: boolean}

    Machine:
      type: object
      additionalProperties: false
      required: [id, name, machineType, status, automaticCollectionEnabled, jobCount, runtimeSeconds, failureRate, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string}
        machineType: {$ref: "#/components/schemas/MachineType"}
        status: {$ref: "#/components/schemas/MachineStatus"}
        externalIdentifier: {type: string, nullable: true}
        automaticCollectionEnabled: {type: boolean}
        lastIngestedAt: {type: string, format: date-time, nullable: true}
        jobCount: {type: integer, format: int64, minimum: 0}
        runtimeSeconds: {type: integer, format: int64, minimum: 0}
        failureRate: {$ref: "#/components/schemas/Decimal"}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    MachinePage:
      type: object
      additionalProperties: false
      required: [items, page, pageSize, total]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/Machine"}}
        page: {type: integer, minimum: 1}
        pageSize: {type: integer, minimum: 1}
        total: {type: integer, format: int64, minimum: 0}

    CreateMachineRequest:
      type: object
      additionalProperties: false
      required: [name, machineTypeId, status, automaticCollectionEnabled]
      properties:
        name: {type: string, minLength: 1, maxLength: 150}
        machineTypeId: {$ref: "#/components/schemas/UUIDv7"}
        status: {$ref: "#/components/schemas/MachineStatus"}
        externalIdentifier: {type: string, maxLength: 200, nullable: true}
        automaticCollectionEnabled: {type: boolean}

    UpdateMachineRequest:
      allOf:
        - $ref: "#/components/schemas/CreateMachineRequest"
        - type: object
          required: [expectedVersion]
          properties:
            expectedVersion: {$ref: "#/components/schemas/Version"}

    OrganizationKind:
      type: string
      enum: [company, institute, association, other]

    Organization:
      type: object
      additionalProperties: false
      required: [id, name, kind, active, pricingGroupAssignmentVersion, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string}
        kind: {$ref: "#/components/schemas/OrganizationKind"}
        active: {type: boolean}
        pricingGroup: {$ref: "#/components/schemas/PricingGroupSummary"}
        pricingGroupAssignmentVersion: {type: integer, format: int64, minimum: 0}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    OrganizationPage:
      type: object
      additionalProperties: false
      required: [items, page, pageSize, total]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/Organization"}}
        page: {type: integer, minimum: 1}
        pageSize: {type: integer, minimum: 1}
        total: {type: integer, format: int64, minimum: 0}

    CreateOrganizationRequest:
      type: object
      additionalProperties: false
      required: [name, kind]
      properties:
        name: {type: string, minLength: 1, maxLength: 200}
        kind: {$ref: "#/components/schemas/OrganizationKind"}

    UpdateOrganizationRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion, name, kind, active]
      properties:
        expectedVersion: {$ref: "#/components/schemas/Version"}
        name: {type: string, minLength: 1, maxLength: 200}
        kind: {$ref: "#/components/schemas/OrganizationKind"}
        active: {type: boolean}

    BillingPartyKind:
      type: string
      enum: [person, organization]

    BillingPartyReference:
      type: object
      additionalProperties: false
      required: [kind, id]
      properties:
        kind: {$ref: "#/components/schemas/BillingPartyKind"}
        id: {$ref: "#/components/schemas/UUIDv7"}

    BillingParty:
      type: object
      additionalProperties: false
      required: [kind, id, displayName, pricingGroupAssignmentVersion]
      properties:
        kind: {$ref: "#/components/schemas/BillingPartyKind"}
        id: {$ref: "#/components/schemas/UUIDv7"}
        displayName: {type: string}
        organizationKind: {$ref: "#/components/schemas/OrganizationKind"}
        pricingGroup: {$ref: "#/components/schemas/PricingGroupSummary"}
        pricingGroupAssignmentVersion: {type: integer, format: int64, minimum: 0}

    BillingPartyList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/BillingParty"}}

    Operator:
      type: object
      additionalProperties: false
      required: [personId, displayName]
      properties:
        personId: {$ref: "#/components/schemas/UUIDv7"}
        displayName: {type: string}

    OperatorList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/Operator"}}

    PricingGroupSummary:
      type: object
      nullable: true
      additionalProperties: false
      required: [id, name]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string}

    PricingRuleKind:
      type: string
      enum: [machine_runtime, material]

    PricingRule:
      type: object
      additionalProperties: false
      required: [id, pricingGroupId, kind, rate, active, version]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        pricingGroupId: {$ref: "#/components/schemas/UUIDv7"}
        kind: {$ref: "#/components/schemas/PricingRuleKind"}
        machineTypeId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        materialCategory: {type: string, nullable: true}
        materialUnit: {allOf: [{$ref: "#/components/schemas/MaterialUnit"}], nullable: true}
        rate: {$ref: "#/components/schemas/Decimal"}
        active: {type: boolean}
        version: {$ref: "#/components/schemas/Version"}

    PricingGroup:
      type: object
      additionalProperties: false
      required: [id, name, active, isDefault, rules, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string}
        description: {type: string, nullable: true}
        active: {type: boolean}
        isDefault: {type: boolean}
        rules: {type: array, items: {$ref: "#/components/schemas/PricingRule"}}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    PricingGroupList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/PricingGroup"}}

    CreatePricingGroupRequest:
      type: object
      additionalProperties: false
      required: [name, isDefault]
      properties:
        name: {type: string, minLength: 1, maxLength: 120}
        description: {type: string, maxLength: 500, nullable: true}
        isDefault: {type: boolean}

    UpdatePricingGroupRequest:
      type: object
      additionalProperties: false
      required: [expectedVersion, name, active, isDefault]
      properties:
        expectedVersion: {$ref: "#/components/schemas/Version"}
        name: {type: string, minLength: 1, maxLength: 120}
        description: {type: string, maxLength: 500, nullable: true}
        active: {type: boolean}
        isDefault: {type: boolean}

    CreatePricingRuleRequest:
      type: object
      additionalProperties: false
      required: [kind, rate]
      properties:
        kind: {$ref: "#/components/schemas/PricingRuleKind"}
        machineTypeId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        materialCategory: {type: string, maxLength: 100, nullable: true}
        materialUnit: {allOf: [{$ref: "#/components/schemas/MaterialUnit"}], nullable: true}
        rate: {$ref: "#/components/schemas/Decimal"}

    UpdatePricingRuleRequest:
      allOf:
        - $ref: "#/components/schemas/CreatePricingRuleRequest"
        - type: object
          required: [expectedVersion, active]
          properties:
            expectedVersion: {$ref: "#/components/schemas/Version"}
            active: {type: boolean}

    SetBillingPartyPricingGroupRequest:
      type: object
      additionalProperties: false
      required: [party, expectedVersion]
      properties:
        party: {$ref: "#/components/schemas/BillingPartyReference"}
        pricingGroupId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        expectedVersion: {type: integer, format: int64, minimum: 0}

    StockState:
      type: string
      enum: [in_stock, low_stock, empty]

    Material:
      type: object
      additionalProperties: false
      required: [id, name, category, unit, active, quantity, averageUnitCost, inventoryValue, stockState, inventoryVersion, recentConsumption, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        name: {type: string}
        category: {type: string}
        color: {type: string, nullable: true}
        unit: {$ref: "#/components/schemas/MaterialUnit"}
        active: {type: boolean}
        lowStockThreshold: {allOf: [{$ref: "#/components/schemas/Decimal"}], nullable: true}
        quantity: {$ref: "#/components/schemas/Decimal"}
        averageUnitCost: {$ref: "#/components/schemas/Decimal"}
        inventoryValue: {$ref: "#/components/schemas/Decimal"}
        stockState: {$ref: "#/components/schemas/StockState"}
        inventoryVersion: {$ref: "#/components/schemas/Version"}
        recentConsumption: {$ref: "#/components/schemas/Decimal"}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    MaterialPage:
      type: object
      additionalProperties: false
      required: [items, page, pageSize, total, totalInventoryValue]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/Material"}}
        page: {type: integer, minimum: 1}
        pageSize: {type: integer, minimum: 1}
        total: {type: integer, format: int64, minimum: 0}
        totalInventoryValue: {$ref: "#/components/schemas/Decimal"}

    CreateMaterialRequest:
      type: object
      additionalProperties: false
      required: [name, category, unit]
      properties:
        name: {type: string, minLength: 1, maxLength: 150}
        category: {type: string, minLength: 1, maxLength: 100}
        color: {type: string, maxLength: 80, nullable: true}
        unit: {$ref: "#/components/schemas/MaterialUnit"}
        lowStockThreshold: {allOf: [{$ref: "#/components/schemas/Decimal"}], nullable: true}

    UpdateMaterialRequest:
      allOf:
        - $ref: "#/components/schemas/CreateMaterialRequest"
        - type: object
          required: [expectedVersion, active]
          properties:
            expectedVersion: {$ref: "#/components/schemas/Version"}
            active: {type: boolean}

    InventoryTransactionKind:
      type: string
      enum: [purchase, machine_job_consumption, manual_consumption, adjustment, disposal]

    InventoryAdjustmentReason:
      type: string
      enum: [inventory_count, unrecorded_consumption, damaged, disposed, mark_empty, job_corrected, other]

    InventoryTransaction:
      type: object
      additionalProperties: false
      required: [id, materialId, kind, quantityDelta, unitAcquisitionCost, inventoryValueDelta, occurredAt, createdAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        materialId: {$ref: "#/components/schemas/UUIDv7"}
        kind: {$ref: "#/components/schemas/InventoryTransactionKind"}
        quantityDelta: {$ref: "#/components/schemas/Decimal"}
        unitAcquisitionCost: {$ref: "#/components/schemas/Decimal"}
        inventoryValueDelta: {$ref: "#/components/schemas/Decimal"}
        totalPurchasePrice: {allOf: [{$ref: "#/components/schemas/Money"}], nullable: true}
        occurredAt: {type: string, format: date-time}
        supplier: {type: string, nullable: true}
        note: {type: string, nullable: true}
        adjustmentReason: {allOf: [{$ref: "#/components/schemas/InventoryAdjustmentReason"}], nullable: true}
        machineJobId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        machineJobDisplayId: {type: string, nullable: true}
        createdAt: {type: string, format: date-time}

    InventoryTransactionPage:
      type: object
      additionalProperties: false
      required: [items, page, pageSize, total]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/InventoryTransaction"}}
        page: {type: integer, minimum: 1}
        pageSize: {type: integer, minimum: 1}
        total: {type: integer, format: int64, minimum: 0}

    InventoryMutationResult:
      type: object
      additionalProperties: false
      required: [material, transaction]
      properties:
        material: {$ref: "#/components/schemas/Material"}
        transaction: {$ref: "#/components/schemas/InventoryTransaction"}

    AddMaterialPurchaseRequest:
      type: object
      additionalProperties: false
      required: [quantity, totalPrice, occurredAt, expectedInventoryVersion]
      properties:
        quantity: {$ref: "#/components/schemas/Decimal"}
        totalPrice: {$ref: "#/components/schemas/Money"}
        occurredAt: {type: string, format: date-time}
        supplier: {type: string, maxLength: 200, nullable: true}
        note: {type: string, maxLength: 1000, nullable: true}
        expectedInventoryVersion: {$ref: "#/components/schemas/Version"}

    RecordMaterialConsumptionRequest:
      type: object
      additionalProperties: false
      required: [quantity, disposal, occurredAt, expectedInventoryVersion]
      properties:
        quantity: {$ref: "#/components/schemas/Decimal"}
        disposal: {type: boolean}
        reason: {allOf: [{$ref: "#/components/schemas/InventoryAdjustmentReason"}], nullable: true}
        occurredAt: {type: string, format: date-time}
        note: {type: string, maxLength: 1000, nullable: true}
        expectedInventoryVersion: {$ref: "#/components/schemas/Version"}

    CorrectMaterialStockRequest:
      type: object
      additionalProperties: false
      required: [physicalQuantity, reason, occurredAt, expectedInventoryVersion]
      properties:
        physicalQuantity: {$ref: "#/components/schemas/Decimal"}
        reason: {$ref: "#/components/schemas/InventoryAdjustmentReason"}
        acquisitionUnitCost: {allOf: [{$ref: "#/components/schemas/Decimal"}], nullable: true}
        occurredAt: {type: string, format: date-time}
        note: {type: string, maxLength: 1000, nullable: true}
        expectedInventoryVersion: {$ref: "#/components/schemas/Version"}

    MarkMaterialEmptyRequest:
      type: object
      additionalProperties: false
      required: [occurredAt, expectedInventoryVersion]
      properties:
        occurredAt: {type: string, format: date-time}
        note: {type: string, maxLength: 1000, nullable: true}
        expectedInventoryVersion: {$ref: "#/components/schemas/Version"}

    MachineJobSource:
      type: string
      enum: [manual, automatic]

    MachineJobReviewState:
      type: string
      enum: [needs_review, confirmed]

    MachineJobOutcome:
      type: string
      enum: [successful, partial_failure, failed, cancelled, unknown]

    PricingStatus:
      type: string
      enum: [pending, complete, incomplete]

    BillingStatus:
      type: string
      enum: [unbilled, billed, waived]

    MachineJobUsageInput:
      type: object
      additionalProperties: false
      required: [materialId, quantity]
      properties:
        materialId: {$ref: "#/components/schemas/UUIDv7"}
        quantity: {$ref: "#/components/schemas/Decimal"}

    MachineJobUsage:
      type: object
      additionalProperties: false
      required: [id, materialId, materialName, category, unit, quantity]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        materialId: {$ref: "#/components/schemas/UUIDv7"}
        materialName: {type: string}
        category: {type: string}
        unit: {$ref: "#/components/schemas/MaterialUnit"}
        quantity: {$ref: "#/components/schemas/Decimal"}

    PricingSnapshotRule:
      type: object
      additionalProperties: false
      required: [kind, label, selector, unit, missing]
      properties:
        sourceRuleId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        kind: {$ref: "#/components/schemas/PricingRuleKind"}
        label: {type: string}
        selector: {type: string}
        unit: {type: string}
        rate: {allOf: [{$ref: "#/components/schemas/Decimal"}], nullable: true}
        missing: {type: boolean}

    MachineJobPricingSnapshot:
      type: object
      nullable: true
      additionalProperties: false
      required: [id, revision, pricingGroupName, currency, reason, complete, rules, capturedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        revision: {type: integer, minimum: 1}
        pricingGroupName: {type: string}
        currency: {type: string, enum: [EUR]}
        reason: {type: string, enum: [initial, customer_changed, machine_changed, manual_reprice]}
        complete: {type: boolean}
        calculatedAmount: {allOf: [{$ref: "#/components/schemas/Money"}], nullable: true}
        rules: {type: array, items: {$ref: "#/components/schemas/PricingSnapshotRule"}}
        capturedAt: {type: string, format: date-time}

    MachineJob:
      type: object
      additionalProperties: false
      required: [id, displayId, machine, startsAt, endsAt, durationSeconds, source, reviewState, outcome, usages, pricingStatus, billingStatus, version, createdAt, updatedAt]
      properties:
        id: {$ref: "#/components/schemas/UUIDv7"}
        displayId: {type: string}
        machine: {$ref: "#/components/schemas/Machine"}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        durationSeconds: {type: integer, format: int64, minimum: 1}
        source: {$ref: "#/components/schemas/MachineJobSource"}
        externalId: {type: string, nullable: true}
        reviewState: {$ref: "#/components/schemas/MachineJobReviewState"}
        customer: {allOf: [{$ref: "#/components/schemas/BillingParty"}], nullable: true}
        operator: {allOf: [{$ref: "#/components/schemas/Operator"}], nullable: true}
        outcome: {$ref: "#/components/schemas/MachineJobOutcome"}
        notes: {type: string, nullable: true}
        usages: {type: array, items: {$ref: "#/components/schemas/MachineJobUsage"}}
        pricingStatus: {$ref: "#/components/schemas/PricingStatus"}
        pricingSnapshot: {$ref: "#/components/schemas/MachineJobPricingSnapshot"}
        calculatedPrice: {allOf: [{$ref: "#/components/schemas/Money"}], nullable: true}
        finalPrice: {allOf: [{$ref: "#/components/schemas/Money"}], nullable: true}
        effectivePrice: {allOf: [{$ref: "#/components/schemas/Money"}], nullable: true}
        priceOverrideReason: {type: string, nullable: true}
        priceOverriddenAt: {type: string, format: date-time, nullable: true}
        billingStatus: {$ref: "#/components/schemas/BillingStatus"}
        billingReference: {type: string, nullable: true}
        version: {$ref: "#/components/schemas/Version"}
        createdAt: {type: string, format: date-time}
        updatedAt: {type: string, format: date-time}

    MachineJobList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/MachineJob"}}

    MachineJobPage:
      type: object
      additionalProperties: false
      required: [items, page, pageSize, total]
      properties:
        items: {type: array, items: {$ref: "#/components/schemas/MachineJob"}}
        page: {type: integer, minimum: 1}
        pageSize: {type: integer, minimum: 1}
        total: {type: integer, format: int64, minimum: 0}

    CreateMachineJobRequest:
      type: object
      additionalProperties: false
      required: [machineId, startsAt, endsAt, customer, operatorPersonId, outcome, usages]
      properties:
        machineId: {$ref: "#/components/schemas/UUIDv7"}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        customer: {$ref: "#/components/schemas/BillingPartyReference"}
        operatorPersonId: {$ref: "#/components/schemas/UUIDv7"}
        outcome: {$ref: "#/components/schemas/MachineJobOutcome"}
        notes: {type: string, maxLength: 2000, nullable: true}
        pricingGroupId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        usages: {type: array, maxItems: 50, items: {$ref: "#/components/schemas/MachineJobUsageInput"}}

    IngestAutomaticMachineJobRequest:
      type: object
      additionalProperties: false
      required: [machineId, externalId, startsAt, endsAt, usages]
      properties:
        machineId: {$ref: "#/components/schemas/UUIDv7"}
        externalId: {type: string, minLength: 1, maxLength: 200}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        usages: {type: array, maxItems: 50, items: {$ref: "#/components/schemas/MachineJobUsageInput"}}
        externalMetadata: {type: object, additionalProperties: true}

    ConfirmMachineJobRequest:
      type: object
      additionalProperties: false
      required: [customer, operatorPersonId, outcome, expectedVersion]
      properties:
        customer: {$ref: "#/components/schemas/BillingPartyReference"}
        operatorPersonId: {$ref: "#/components/schemas/UUIDv7"}
        outcome: {$ref: "#/components/schemas/MachineJobOutcome"}
        notes: {type: string, maxLength: 2000, nullable: true}
        pricingGroupId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    UpdateMachineJobRequest:
      type: object
      additionalProperties: false
      required: [machineId, startsAt, endsAt, customer, operatorPersonId, outcome, expectedVersion]
      properties:
        machineId: {$ref: "#/components/schemas/UUIDv7"}
        startsAt: {type: string, format: date-time}
        endsAt: {type: string, format: date-time}
        customer: {$ref: "#/components/schemas/BillingPartyReference"}
        operatorPersonId: {$ref: "#/components/schemas/UUIDv7"}
        outcome: {$ref: "#/components/schemas/MachineJobOutcome"}
        notes: {type: string, maxLength: 2000, nullable: true}
        pricingGroupId: {allOf: [{$ref: "#/components/schemas/UUIDv7"}], nullable: true}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    ReplaceMachineJobUsagesRequest:
      type: object
      additionalProperties: false
      required: [usages, expectedVersion]
      properties:
        usages: {type: array, maxItems: 50, items: {$ref: "#/components/schemas/MachineJobUsageInput"}}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    OverrideMachineJobPriceRequest:
      type: object
      additionalProperties: false
      required: [finalPrice, reason, expectedVersion]
      properties:
        finalPrice: {$ref: "#/components/schemas/Money"}
        reason: {type: string, minLength: 1, maxLength: 500}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    UpdateMachineJobBillingRequest:
      type: object
      additionalProperties: false
      required: [status, expectedVersion]
      properties:
        status: {$ref: "#/components/schemas/BillingStatus"}
        billingReference: {type: string, maxLength: 200, nullable: true}
        waiverReason: {type: string, maxLength: 500, nullable: true}
        expectedVersion: {$ref: "#/components/schemas/Version"}

    DailyJobActivity:
      type: object
      additionalProperties: false
      required: [date, count]
      properties:
        date: {type: string, format: date}
        count: {type: integer, format: int64, minimum: 0}

    MachineLogbookOverview:
      type: object
      additionalProperties: false
      required: [jobsToday, jobsThisWeek, unbilledJobs, unbilledAmount, needsReview, failedOrPartialThisWeek, lowStockItems, recentJobs, lowStockMaterials, activity]
      properties:
        jobsToday: {type: integer, format: int64, minimum: 0}
        jobsThisWeek: {type: integer, format: int64, minimum: 0}
        unbilledJobs: {type: integer, format: int64, minimum: 0}
        unbilledAmount: {$ref: "#/components/schemas/Money"}
        needsReview: {type: integer, format: int64, minimum: 0}
        failedOrPartialThisWeek: {type: integer, format: int64, minimum: 0}
        lowStockItems: {type: integer, format: int64, minimum: 0}
        recentJobs: {type: array, items: {$ref: "#/components/schemas/MachineJob"}}
        lowStockMaterials: {type: array, items: {$ref: "#/components/schemas/Material"}}
        activity: {type: array, items: {$ref: "#/components/schemas/DailyJobActivity"}}

    StatisticPoint:
      type: object
      additionalProperties: false
      required: [key, label, value]
      properties:
        key: {type: string}
        label: {type: string}
        value: {$ref: "#/components/schemas/Decimal"}

    MachineLogbookStatistics:
      type: object
      additionalProperties: false
      required: [materialUsageByCategory, machineRuntimeHours, machineJobCounts, acquisitionCost, customerCharges, adjustmentLoss, machineFailureRates]
      properties:
        materialUsageByCategory: {type: array, items: {$ref: "#/components/schemas/StatisticPoint"}}
        machineRuntimeHours: {type: array, items: {$ref: "#/components/schemas/StatisticPoint"}}
        machineJobCounts: {type: array, items: {$ref: "#/components/schemas/StatisticPoint"}}
        acquisitionCost: {type: array, items: {$ref: "#/components/schemas/StatisticPoint"}}
        customerCharges: {type: array, items: {$ref: "#/components/schemas/StatisticPoint"}}
        adjustmentLoss: {type: array, items: {$ref: "#/components/schemas/StatisticPoint"}}
        machineFailureRates: {type: array, items: {$ref: "#/components/schemas/StatisticPoint"}}
