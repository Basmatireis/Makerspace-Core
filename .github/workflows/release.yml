name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  tag:
    name: Release tag
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Validate stable semantic version
        id: version
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          if [[ ! "$RELEASE_TAG" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)$ ]]; then
            echo "Release tags must have the form vMAJOR.MINOR.PATCH."
            exit 1
          fi
          echo "version=${RELEASE_TAG#v}" >> "$GITHUB_OUTPUT"

  validate:
    name: Validate
    needs: tag
    permissions:
      contents: read
    uses: ./.github/workflows/ci.yml

  immutable:
    name: Immutable version
    needs: tag
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Refuse to overwrite version tags
        env:
          IMAGE_PREFIX: ghcr.io/${{ github.repository }}
          VERSION: ${{ needs.tag.outputs.version }}
        run: |
          image_prefix="${IMAGE_PREFIX,,}"
          for component in backend frontend; do
            image="${image_prefix}-${component}:${VERSION}"
            output_file="${RUNNER_TEMP}/${component}-manifest-check.txt"
            if docker buildx imagetools inspect "$image" >"$output_file" 2>&1; then
              echo "The immutable image tag already exists: $image"
              exit 1
            fi
            if ! grep -Eqi 'manifest unknown|name unknown|not found' "$output_file"; then
              cat "$output_file"
              echo "Could not safely confirm that $image is unused."
              exit 1
            fi
          done

  publish:
    name: Publish (${{ matrix.component }})
    needs: [tag, validate, immutable]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - component: backend
            dockerfile: backend/Dockerfile
          - component: frontend
            dockerfile: frontend/Dockerfile
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Generate image metadata
        id: metadata
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}-${{ matrix.component }}
          flavor: latest=false
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}},enable=${{ !startsWith(github.ref, 'refs/tags/v0.') }}
            type=raw,value=latest
      - name: Build and publish image
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ${{ matrix.dockerfile }}
          target: final
          push: true
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
          annotations: ${{ steps.metadata.outputs.annotations }}
          cache-from: type=gha,scope=${{ matrix.component }}
          cache-to: type=gha,mode=max,scope=${{ matrix.component }}
      - name: Record published digest
        env:
          COMPONENT: ${{ matrix.component }}
          DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          echo "### ${COMPONENT} image" >> "$GITHUB_STEP_SUMMARY"
          echo "Digest: \`${DIGEST}\`" >> "$GITHUB_STEP_SUMMARY"

  public-images:
    name: Public release images
    needs: [tag, publish]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Verify anonymous image access
        env:
          IMAGE_PREFIX: ghcr.io/${{ github.repository }}
          VERSION: ${{ needs.tag.outputs.version }}
        run: |
          image_prefix="${IMAGE_PREFIX,,}"
          for component in backend frontend; do
            image="${image_prefix}-${component}:${VERSION}"
            available=false
            for attempt in 1 2 3 4 5 6; do
              if docker buildx imagetools inspect "$image" >/dev/null; then
                available=true
                break
              fi
              sleep 10
            done
            if [ "$available" != true ]; then
              echo "Release image is not anonymously pullable: $image"
              echo "Set both GHCR packages to public before creating a release."
              exit 1
            fi
          done

  github-release:
    name: GitHub Release
    needs: [tag, public-images]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out repository
        uses: actions/checkout@v7
      - name: Package production Compose bundle
        id: bundle
        env:
          VERSION: ${{ needs.tag.outputs.version }}
        run: |
          archive_path="$(./scripts/package-production-compose.sh "$VERSION" "$RUNNER_TEMP")"
          echo "archive_path=$archive_path" >> "$GITHUB_OUTPUT"
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.ref_name }}
          COMPOSE_BUNDLE: ${{ steps.bundle.outputs.archive_path }}
        run: gh release create "$RELEASE_TAG" "$COMPOSE_BUNDLE#Production Compose bundle" --verify-tag --generate-notes --title "$RELEASE_TAG"
