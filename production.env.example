# Copy this file to .env beside the release bundle's compose.yaml. Do not commit
# the populated file. The release bundle already pins its application version.
COMPOSE_PROJECT_NAME=makerspace-core
# MAKERSPACE_IMAGE_PREFIX=ghcr.io/basmatireis/makerspace-core

# Exact browser-visible HTTPS origin. Do not include a path or trailing slash.
PUBLIC_BASE_URL=

# Only the frontend is published. The loopback default is intended for a TLS
# reverse proxy running on the Docker host.
FRONTEND_BIND_ADDRESS=127.0.0.1
FRONTEND_PORT=8080

# PostgreSQL remains reachable only on the Compose network. Generate a long
# random password using URL-safe characters so the environment file stays easy
# to parse; the password is passed separately rather than embedded in a URL.
POSTGRES_DB=makerspace
POSTGRES_USER=makerspace
POSTGRES_PASSWORD=

# Backend runtime policy.
# Comma-separated CIDRs of proxies that connect directly to the backend. Leave
# unset unless the corresponding networks are restricted to trusted proxies.
# HTTP_TRUSTED_PROXIES=10.20.0.0/24,2001:db8:1234::/48
SESSION_IDLE_TTL=6h
SESSION_ABSOLUTE_TTL=72h
PASSWORD_RESET_TTL=30m
# Base64-encoded secret of at least 32 random bytes.
AUTH_CHALLENGE_HMAC_KEY=
PIN_PEPPER=
# Ordered comma-separated base64 32-byte AES keys; the first encrypts new OIDC secrets.
APP_ENCRYPTION_KEYS=
STORAGE_BACKEND=local
LOCAL_STORAGE_ROOT=/var/lib/makerspace/files
# S3_ENDPOINT=
# S3_REGION=
# S3_BUCKET=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_USE_PATH_STYLE=false
# S3_DISABLE_TLS=false
AUDIT_RETENTION=8760h

# Open Days local-time and offline public-holiday context. Defaults are for Graz.
MAKERSPACE_TIME_ZONE=Europe/Vienna
OPEN_DAYS_HOLIDAY_COUNTRY=AT
OPEN_DAYS_HOLIDAY_SUBDIVISION=AT-6
OPEN_DAYS_HOLIDAY_LANGUAGE=de

# Optional OpenTelemetry export. Leave disabled when there is no collector.
OTEL_SDK_DISABLED=true
# OTEL_EXPORTER_OTLP_ENDPOINT=http://alloy.example.internal:4318
# OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
OTEL_SERVICE_NAME=makerspace-core-api
