/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Makerspace Core API
 * HTTP contract for one makerspace deployment. OpenAPI is the transport source
of truth; authorization and domain invariants remain backend responsibilities.

Optional nullable fields deliberately distinguish three states: an omitted
property is unavailable or unchanged, `null` clears or represents no value,
and a non-null value is present. In particular, `matriculationNumber` is
omitted from responses unless the caller has `people.read.matriculation`.

Every session-authenticated operation also accepts the optional
`X-Managed-Device-Token` credential declared below. OpenAPI security
requirements cannot express an optional second credential without changing
their boolean meaning, so the server evaluates this header independently of
the required session and CSRF credentials.

 * OpenAPI spec version: 0.1.0
 */
import type {
  CreateRoleRequest,
  ListRolesParams,
  ReplaceRolePermissionsRequest,
  Role,
  RolePage,
  UpdateRoleRequest,
  VersionRequest
} from '.././models';

import { apiFetch } from '../../http-client';

/**
 * Requires `roles.read`.
 * @summary List configured roles
 */
export const getListRolesUrl = (params?: ListRolesParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v1/roles?${stringifiedParams}` : `/api/v1/roles`
}

export const listRoles = async (params?: ListRolesParams, options?: RequestInit): Promise<RolePage> => {
  
  return apiFetch<RolePage>(getListRolesUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}


/**
 * Requires `roles.manage`; a non-master actor may grant only permissions they currently hold.
 * @summary Create a configurable role
 */
export const getCreateRoleUrl = () => {


  

  return `/api/v1/roles`
}

export const createRole = async (createRoleRequest: CreateRoleRequest, options?: RequestInit): Promise<Role> => {
  
  return apiFetch<Role>(getCreateRoleUrl(),
  {      
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      createRoleRequest,)
  }
);}


/**
 * Requires `roles.read`.
 * @summary Get one role
 */
export const getGetRoleUrl = (roleId: string,) => {


  

  return `/api/v1/roles/${roleId}`
}

export const getRole = async (roleId: string, options?: RequestInit): Promise<Role> => {
  
  return apiFetch<Role>(getGetRoleUrl(roleId),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}


/**
 * Requires `roles.manage`. The master role cannot be renamed or edited.
 * @summary Update role metadata
 */
export const getUpdateRoleUrl = (roleId: string,) => {


  

  return `/api/v1/roles/${roleId}`
}

export const updateRole = async (roleId: string,
    updateRoleRequest: UpdateRoleRequest, options?: RequestInit): Promise<Role> => {
  
  return apiFetch<Role>(getUpdateRoleUrl(roleId),
  {      
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      updateRoleRequest,)
  }
);}


/**
 * Requires `roles.manage`. Deletion removes the role's assignments and permission rows; the master role cannot be deleted.
 * @summary Delete a configurable role
 */
export const getDeleteRoleUrl = (roleId: string,) => {


  

  return `/api/v1/roles/${roleId}`
}

export const deleteRole = async (roleId: string,
    versionRequest: VersionRequest, options?: RequestInit): Promise<void> => {
  
  return apiFetch<void>(getDeleteRoleUrl(roleId),
  {      
    ...options,
    method: 'DELETE',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      versionRequest,)
  }
);}


/**
 * Requires `roles.manage`. Unknown permission IDs are rejected. A non-master
actor may grant only a subset of their effective permissions. The master
role's effective permissions always come from application code and cannot
be edited here.

 * @summary Replace a configurable role's permissions
 */
export const getReplaceRolePermissionsUrl = (roleId: string,) => {


  

  return `/api/v1/roles/${roleId}/permissions`
}

export const replaceRolePermissions = async (roleId: string,
    replaceRolePermissionsRequest: ReplaceRolePermissionsRequest, options?: RequestInit): Promise<Role> => {
  
  return apiFetch<Role>(getReplaceRolePermissionsUrl(roleId),
  {      
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      replaceRolePermissionsRequest,)
  }
);}


