/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Makerspace Core API
 * HTTP contract for one makerspace deployment. OpenAPI is the transport source
of truth; authorization and domain invariants remain backend responsibilities.

Optional nullable fields deliberately distinguish three states: an omitted
property is unavailable or unchanged, `null` clears or represents no value,
and a non-null value is present. In particular, `matriculationNumber` is
omitted from responses unless the caller has `people.read.matriculation`.

Every session-authenticated operation also accepts the optional
`X-Managed-Device-Token` credential declared below. OpenAPI security
requirements cannot express an optional second credential without changing
their boolean meaning, so the server evaluates this header independently of
the required session and CSRF credentials.

 * OpenAPI spec version: 0.1.0
 */
import type {
  CreatePersonRequest,
  ListPeopleParams,
  Person,
  PersonPage,
  UpdatePersonRequest,
  VersionRequest
} from '.././models';

import { apiFetch } from '../../http-client';

/**
 * Requires `people.read.all`; account data is omitted without `accounts.read`.
 * @summary List people for user administration
 */
export const getListPeopleUrl = (params?: ListPeopleParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString())
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v1/people?${stringifiedParams}` : `/api/v1/people`
}

export const listPeople = async (params?: ListPeopleParams, options?: RequestInit): Promise<PersonPage> => {
  
  return apiFetch<PersonPage>(getListPeopleUrl(params),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}


/**
 * Requires `people.create`; matriculation input additionally requires `people.update.matriculation`.
 * @summary Create a person
 */
export const getCreatePersonUrl = () => {


  

  return `/api/v1/people`
}

export const createPerson = async (createPersonRequest: CreatePersonRequest, options?: RequestInit): Promise<Person> => {
  
  return apiFetch<Person>(getCreatePersonUrl(),
  {      
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      createPersonRequest,)
  }
);}


/**
 * Requires the applicable `people.read.self` or `people.read.all` permission.
 * @summary Get one person
 */
export const getGetPersonUrl = (personId: string,) => {


  

  return `/api/v1/people/${personId}`
}

export const getPerson = async (personId: string, options?: RequestInit): Promise<Person> => {
  
  return apiFetch<Person>(getGetPersonUrl(personId),
  {      
    ...options,
    method: 'GET'
    
    
  }
);}


/**
 * Requires the applicable `people.update.self` or `people.update.all`
permission. Sending `matriculationNumber` additionally requires
`people.update.matriculation`. Omitted fields are unchanged; nullable
fields sent as `null` are cleared.

 * @summary Update one person
 */
export const getUpdatePersonUrl = (personId: string,) => {


  

  return `/api/v1/people/${personId}`
}

export const updatePerson = async (personId: string,
    updatePersonRequest: UpdatePersonRequest, options?: RequestInit): Promise<Person> => {
  
  return apiFetch<Person>(getUpdatePersonUrl(personId),
  {      
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      updatePersonRequest,)
  }
);}


/**
 * Requires `people.delete`. If the person has an account, also requires
`accounts.delete`. Audit references remain non-PII identifiers and do not
prevent hard deletion.

 * @summary Permanently delete one person
 */
export const getDeletePersonUrl = (personId: string,) => {


  

  return `/api/v1/people/${personId}`
}

export const deletePerson = async (personId: string,
    versionRequest: VersionRequest, options?: RequestInit): Promise<void> => {
  
  return apiFetch<void>(getDeletePersonUrl(personId),
  {      
    ...options,
    method: 'DELETE',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(
      versionRequest,)
  }
);}


